# حزمة توثيق OmniRAG

- Version: v1
- Documents: 9/9

# 📋 وثيقة وصف المنتج (PRD) — تطبيق Hybrid RAG ثنائي اللغة

---

## 🏷️ اسم المشروع المقترح: **OmniRAG — منصة الاسترجاع التوليدي الهجين الذكية**

---

## 1. الرؤية العامة (Product Vision)

تطبيق **Hybrid RAG (Retrieval-Augmented Generation)** متقدم وثنائي اللغة (عربي/إنجليزي) يُمكّن المستخدمين من جلب بياناتهم من مصادر متعددة ومتنوعة، ومعالجتها بذكاء اصطناعي متطور، ثم الاستعلام عنها بلغة طبيعية مع ضمان **العزل الكامل** و**الخصوصية التامة** لكل مستخدم. يجمع النظام بين **البحث الدلالي** (Semantic Search) و**البحث المعجمي** (Lexical/Keyword Search) في بنية هجينة لتحقيق أعلى دقة في الاسترجاع.

---

## 2. المكدس التقني (Technology Stack)

### 2.1 نماذج الذكاء الاصطناعي

| الطبقة | النموذج | الوصف |
|---|---|---|
| **التضمين (Embedding)** | `gemini-embedding-2` | نموذج تضمين متعدد الوسائط يدعم تضمين الصور والفيديو والصوت والمستندات إلى جانب النصوص. يُنتج متجهات بأبعاد 3072. يلتقط المعنى الدلالي عبر أكثر من 100 لغة |
| **الاستدلال السريع** | `gemini-3.5-flash-lite` | نموذج متعدد الوسائط منخفض التأخير وفعّال التكلفة محسّن للإنتاجية العالية ومهام التحليل السريع. أسرع نموذج في فئة 3.5 بسرعة 350 رمزاً في الثانية |
| **الاستدلال المتقدم** | `gemini-3.6-flash` | نموذج من سلسلة Gemini 3 عالي القدرة للاستدلال متعدد الوسائط، يقدم أداءً أفضل في البرمجة والعمل المعرفي مع كفاءة أعلى في استهلاك الرموز مقارنة بـ Gemini 3.5 Flash |

### 2.2 معالجة المستندات (Document Processing)

| الأداة | الوصف |
|---|---|
| **Mistral Document AI (OCR 4)** | يوفر معالجة مستندات على مستوى المؤسسات تجمع بين تقنية OCR واستخراج البيانات المنظمة المتقدم، مع دعم متعدد اللغات وتعليقات توضيحية وسير عمل قابلة للتكيف لجميع أنواع المستندات. يدعم 170 لغة مع صناديق إحاطة وتصنيف كتل ودرجات ثقة |
| **Unstructured Transform API** | يجلب معالجة مستندات بمستوى إنتاجي كخادم MCP مع القدرة على تحويل أكثر من 60 نوع ملف إلى بيانات منظمة. يوفر مكونات مفتوحة المصدر لاستيعاب ومعالجة الصور والمستندات النصية مثل PDF وHTML وWord وغيرها، حيث تدور حالات الاستخدام حول تبسيط وتحسين سير عمل معالجة البيانات لنماذج اللغة الكبيرة |

### 2.3 قواعد البيانات

| قاعدة البيانات | الدور |
|---|---|
| **Qdrant** | قاعدة بيانات متجهية عالية الأداء للبحث الدلالي (Vector Search) — تُخزن التضمينات وتُجري البحث بأقرب الجيران (ANN) |
| **Neon Postgres** | قاعدة بيانات علائقية Serverless تعتمد على PostgreSQL — تُخزن البيانات الوصفية (Metadata)، وبيانات المستخدمين، وسجلات المحادثات، والإعدادات، مع دعم `pgvector` للبحث الهجين |

### 2.4 النشر والبنية التحتية

| المكوّن | التقنية |
|---|---|
| **الاستضافة** | Vercel (Edge Functions + Serverless) |
| **الإطار** | Next.js 16+ (App Router) مع React Server Components |
| **اللغة** | TypeScript |
| **المصادقة** | NextAuth.js / Clerk مع Row-Level Security (RLS) |
| **إدارة المهام الخلفية** | Inngest / Trigger.dev للمهام غير المتزامنة |
| **التخزين المؤقت** | Vercel KV (Redis) |
| **تخزين الملفات** | Vercel Blob / S3-Compatible Storage |

---

## 3. البنية المعمارية الشاملة (System Architecture)

```
┌─────────────────────────────────────────────────────────────────────┐
│                        🌐 طبقة العرض (Presentation Layer)            │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐  │
│  │  لوحة    │ │  محادثة  │ │  مصادر   │ │ إعدادات  │ │ إدارة    │  │
│  │  التحكم  │ │  ذكية    │ │  البيانات│ │ المستخدم │ │ المعرفة  │  │
│  └────┬─────┘ └────┬─────┘ └────┬─────┘ └────┬─────┘ └────┬─────┘  │
│       │            │            │            │            │         │
├───────┴────────────┴────────────┴────────────┴────────────┴─────────┤
│                     🔌 طبقة API (API Layer - Next.js API Routes)     │
│  ┌───────────────────────────────────────────────────────────────┐  │
│  │  Rate Limiting │ Auth Middleware │ Tenant Isolation │ CORS    │  │
│  └───────────────────────────────────────────────────────────────┘  │
├────────────────────────────────────────────────────────────────────┤
│                 🧠 طبقة المنطق الأساسي (Core Logic Layer)           │
│                                                                     │
│  ┌─────────────┐  ┌──────────────┐  ┌────────────────────────────┐ │
│  │  Ingestion   │  │  RAG Engine   │  │  Query Processing Pipeline │ │
│  │  Pipeline    │  │  (Hybrid)     │  │                            │ │
│  │             │  │              │  │  ┌────────┐ ┌────────────┐ │ │
│  │ • Mistral   │  │ • Semantic   │  │  │ Query  │→│ Intent     │ │ │
│  │   Doc AI    │  │   Search     │  │  │ Parser │ │ Detection  │ │ │
│  │ • Unstruct. │  │ • Lexical    │  │  └────────┘ └─────┬──────┘ │ │
│  │   Transform │  │   Search     │  │               ┌───▼──────┐ │ │
│  │ • Chunking  │  │ • Reciprocal │  │               │ Routing  │ │ │
│  │ • Embedding │  │   Rank Fusion│  │               │ Engine   │ │ │
│  └─────────────┘  └──────────────┘  │               └──────────┘ │ │
│                                      └────────────────────────────┘ │
├────────────────────────────────────────────────────────────────────┤
│                   💾 طبقة البيانات (Data Layer)                      │
│  ┌────────────────┐  ┌────────────────┐  ┌─────────────────────┐   │
│  │   Qdrant        │  │  Neon Postgres  │  │  Vercel Blob/S3     │   │
│  │   (Vectors)     │  │  (Metadata+RLS) │  │  (Raw Files)        │   │
│  └────────────────┘  └────────────────┘  └─────────────────────┘   │
└────────────────────────────────────────────────────────────────────┘
```

---

## 4. الصفحات والمكوّنات التفصيلية (Pages & Components)

### 📄 4.1 صفحة التسجيل والمصادقة (`/auth`)

| المكوّن | الوصف |
|---|---|
| `LoginForm` | تسجيل الدخول بالبريد الإلكتروني/كلمة المرور أو OAuth (Google, GitHub) |
| `RegisterForm` | إنشاء حساب جديد مع تأكيد البريد الإلكتروني |
| `TenantCreation` | إنشاء مساحة عمل معزولة تلقائياً عند التسجيل الأول |
| `MFASetup` | إعداد المصادقة الثنائية (اختياري) |

**آلية العزل:**
- كل مستخدم يحصل على `tenant_id` فريد
- جميع استعلامات قاعدة البيانات تُفلتر تلقائياً عبر Row-Level Security (RLS) في Neon Postgres
- كل مجموعة (Collection) في Qdrant تُنشأ لكل مستخدم منفصلةً مع فلترة Payload بـ `tenant_id`

---

### 📄 4.2 لوحة التحكم الرئيسية (`/dashboard`)

| المكوّن | الوصف |
|---|---|
| `StatsOverview` | إحصائيات سريعة: عدد المستندات، الأجزاء المُفهرسة، الاستعلامات اليومية، مساحة التخزين المستخدمة |
| `RecentChats` | آخر المحادثات مع إمكانية الاستمرار فيها |
| `IngestionStatus` | حالة عمليات الاستيعاب الجارية (قيد المعالجة، مكتملة، فاشلة) |
| `QuotaUsage` | استهلاك الحصص: رموز API، مساحة التخزين، عدد الاستعلامات |
| `QuickActions` | أزرار وصول سريع: رفع مستند، محادثة جديدة، إضافة مصدر |
| `ActivityTimeline` | خط زمني لآخر الأنشطة والعمليات |
| `LanguageToggle` | تبديل واجهة المستخدم بين العربية والإنجليزية (مع دعم RTL الكامل) |

---

### 📄 4.3 صفحة مصادر البيانات (`/sources`)

مركز التحكم بجميع مصادر جلب البيانات. يدعم التطبيق المصادر التالية:

#### 📁 مصادر رفع الملفات المباشر
| النوع | التفاصيل |
|---|---|
| **PDF** | دعم PDF الرقمي والممسوح ضوئياً (Scanned) |
| **المستندات النصية** | DOCX, DOC, TXT, RTF, ODT |
| **جداول البيانات** | XLSX, CSV, TSV |
| **العروض التقديمية** | PPTX, PPT |
| **الصور** | PNG, JPG, WEBP (مع استخراج النص بـ OCR) |
| **الصوت** | MP3, WAV, M4A (تحويل إلى نص ثم تضمين) |
| **الفيديو** | MP4, MOV (استخراج الصوت والنص والإطارات) |
| **الترميز** | Markdown, HTML, JSON, XML, YAML |
| **الكتب الإلكترونية** | EPUB |

#### 🌐 مصادر الويب والتكاملات
| المصدر | التفاصيل |
|---|---|
| **روابط URL** | زحف صفحة واحدة أو موقع كامل (Web Crawler) مع دعم Sitemap |
| **خلاصات RSS/Atom** | جلب تلقائي دوري من المدونات والأخبار |
| **YouTube** | استخراج النص التلقائي (Transcript) والتعليقات |
| **GitHub/GitLab** | فهرسة المستودعات (README, Issues, Wiki, Code) |
| **Notion** | مزامنة الصفحات وقواعد البيانات عبر API |
| **Google Drive** | مزامنة الملفات والمجلدات المحددة |
| **Confluence** | جلب المساحات والصفحات |
| **Slack/Discord** | فهرسة القنوات المحددة |
| **البريد الإلكتروني** | IMAP/Gmail API — فهرسة مجلدات محددة |
| **قواعد بيانات خارجية** | PostgreSQL, MySQL, MongoDB عبر موصلات مخصصة |
| **API مخصص** | إدخال Endpoint لجلب بيانات من أي واجهة REST/GraphQL |

#### مكوّنات صفحة المصادر:
| المكوّن | الوصف |
|---|---|
| `SourceCard` | بطاقة لكل مصدر تعرض: النوع، الحالة، عدد المستندات، آخر مزامنة |
| `AddSourceModal` | نافذة إضافة مصدر جديد مع معالج خطوة بخطوة (Wizard) |
| `FileUploadZone` | منطقة سحب وإفلات متعددة الملفات مع شريط تقدم |
| `SyncScheduler` | جدولة المزامنة التلقائية (كل ساعة / يومياً / أسبوعياً / مخصص) |
| `SourceHealthMonitor` | مراقبة صحة الاتصال بالمصادر مع تنبيهات عند الفشل |
| `BulkImporter` | استيراد مجموعة كبيرة من الملفات أو الروابط دفعة واحدة |

---

### 📄 4.4 صفحة المحادثة الذكية (`/chat`)

الواجهة الرئيسية للتفاعل مع البيانات عبر لغة طبيعية.

| المكوّن | الوصف |
|---|---|
| `ChatInterface` | واجهة محادثة تشبه ChatGPT مع دعم RTL الكامل للعربية |
| `MessageBubble` | فقاعات رسائل تدعم Markdown, Syntax Highlighting, جداول, LaTeX |
| `SourceCitations` | عرض المراجع المصدرية لكل إجابة مع روابط للمستند الأصلي ورقم الصفحة |
| `ConfidenceScore` | مؤشر بصري لدرجة ثقة الإجابة |
| `ChatModeSelector` | مُبدّل أوضاع المحادثة (انظر أدناه) |
| `StreamingRenderer` | عرض الإجابات بالتدفق المباشر (Streaming) |
| `ChatHistory` | قائمة جانبية بالمحادثات السابقة مع بحث وتصنيف |
| `FollowUpSuggestions` | اقتراحات أسئلة متابعة ذكية |
| `ExportChat` | تصدير المحادثة بصيغة PDF/Markdown |
| `ModelSelector` | اختيار نموذج الاستدلال (Flash-Lite للسرعة / Flash للجودة) |
| `MultimodalInput` | إدخال نصوص + صور + ملفات مباشرة في المحادثة |

#### أوضاع المحادثة (Chat Modes):

```
┌──────────────────────────────────────────────────────────────┐
│                    🎯 أوضاع العمل                            │
│                                                              │
│  ┌──────────────────┐  ┌──────────────────────────────────┐  │
│  │ 🔒 وضع مقيّد      │  │  الإجابة فقط من بيانات المستخدم   │  │
│  │ (Private RAG)    │  │  الخاصة — دقة عالية، لا هلوسات    │  │
│  └──────────────────┘  └──────────────────────────────────┘  │
│                                                              │
│  ┌──────────────────┐  ┌──────────────────────────────────┐  │
│  │ 🌐 وضع هجين      │  │  البيانات الخاصة كأساس + بحث     │  │
│  │ (Hybrid Web+RAG) │  │  الويب لتوسيع الإجابة وتحديثها  │  │
│  └──────────────────┘  └──────────────────────────────────┘  │
│                                                              │
│  ┌──────────────────┐  ┌──────────────────────────────────┐  │
│  │ 💬 وضع محادثة     │  │  محادثة عامة مع النموذج بدون     │  │
│  │ (General Chat)   │  │  استرجاع — للأسئلة العامة        │  │
│  └──────────────────┘  └──────────────────────────────────┘  │
│                                                              │
│  ┌──────────────────┐  ┌──────────────────────────────────┐  │
│  │ 📊 وضع التحليل    │  │  تحليل مستند محدد أو مجموعة     │  │
│  │ (Analysis Mode)  │  │  مستندات بعمق مع تلخيص ومقارنة  │  │
│  └──────────────────┘  └──────────────────────────────────┘  │
└──────────────────────────────────────────────────────────────┘
```

---

### 📄 4.5 صفحة إدارة قاعدة المعرفة (`/knowledge`)

| المكوّن | الوصف |
|---|---|
| `CollectionManager` | إنشاء وإدارة مجموعات معرفية (Collections) منفصلة بحسب المشاريع أو المواضيع |
| `DocumentExplorer` | تصفح المستندات المُفهرسة مع معاينة المحتوى |
| `ChunkViewer` | عرض الأجزاء (Chunks) المُقسمة لكل مستند مع التضمينات |
| `ChunkEditor` | تعديل أو حذف أو دمج أجزاء يدوياً |
| `SemanticSearchTest` | أداة اختبار البحث الدلالي — إدخال استعلام ورؤية النتائج مُرتبة |
| `DocumentRelationGraph` | رسم بياني تفاعلي يُظهر العلاقات بين المستندات |
| `TagManager` | إدارة التصنيفات والعلامات (Tags) |
| `BulkActions` | عمليات جماعية: حذف، إعادة تضمين، نقل بين مجموعات |
| `EmbeddingVisualizer` | تصور ثلاثي الأبعاد للتضمينات بتقنية t-SNE/UMAP |

---

### 📄 4.6 صفحة الإعدادات (`/settings`)

#### 4.6.1 إعدادات معالجة المستندات

```
┌──────────────────────────────────────────────────────┐
│        ⚙️ محرك معالجة المستندات                       │
│                                                      │
│  ○ Mistral Document AI (OCR 4)                       │
│    ├─ دقة عالية في OCR                               │
│    ├─ 170 لغة                                        │
│    ├─ صناديق إحاطة + تصنيف كتل                      │
│    └─ الأفضل لـ: PDF ممسوح، مستندات معقدة           │
│                                                      │
│  ○ Unstructured Transform API                        │
│    ├─ 60+ نوع ملف                                    │
│    ├─ تقسيم ذكي + إثراء                              │
│    ├─ موصلات مدمجة                                   │
│    └─ الأفضل لـ: تنوع أنواع الملفات، معالجة دُفعات  │
│                                                      │
│  ○ تلقائي (Auto) — يختار المحرك الأنسب حسب نوع      │
│    الملف ومحتواه تلقائياً                             │
│                                                      │
│  [حفظ الإعدادات]                                     │
└──────────────────────────────────────────────────────┘
```

**تفصيل خيارات Mistral Document AI:**
- يستخرج النص مع الحفاظ على بنية المستند وتسلسله الهرمي ويحافظ على التنسيق مثل العناوين والفقرات والقوائم والجداول، مع إمكانية تبديل تنسيق الجداول بين markdown وHTML
- يقدم صناديق إحاطة وتصنيف كتل ودرجات ثقة مضمّنة لكل منطقة

**تفصيل خيارات Unstructured Transform:**
- يدعم التقسيم والتضمين وإثراء الصور والجداول من واجهة برمجية أو واجهة مستخدم منخفضة التعليمات البرمجية
- يفرض التحكم في الوصول على مستوى الأجزاء (Chunk-level) بحيث يحمل كل جزء أذونات المستند الأصلي

#### 4.6.2 إعدادات التقسيم (Chunking Settings)

| الإعداد | الوصف |
|---|---|
| `chunkSize` | حجم الجزء (256 — 2048 رمز) — قابل للتعديل بمنزلق |
| `chunkOverlap` | نسبة التداخل بين الأجزاء (0% — 30%) |
| `chunkingStrategy` | استراتيجية التقسيم: حسب الفقرات / حسب الجمل / حسب العناوين / دلالي (Semantic) / هجين |
| `preserveStructure` | الحفاظ على بنية الجداول والقوائم أثناء التقسيم |
| `languageDetection` | كشف اللغة التلقائي لكل جزء (عربي/إنجليزي/مختلط) |

#### 4.6.3 إعدادات البحث الهجين (Hybrid Search Configuration)

| الإعداد | الوصف |
|---|---|
| `semanticWeight` | وزن البحث الدلالي (0.0 — 1.0) |
| `lexicalWeight` | وزن البحث المعجمي (0.0 — 1.0) |
| `fusionAlgorithm` | خوارزمية الدمج: Reciprocal Rank Fusion (RRF) / Weighted Sum / Convex Combination |
| `topK` | عدد النتائج المُسترجعة (1 — 20) |
| `reranking` | تفعيل إعادة الترتيب (Re-ranking) باستخدام نموذج Cross-Encoder |
| `scoreThreshold` | الحد الأدنى لدرجة التطابق لعرض النتائج |
| `mmrDiversity` | تنوع النتائج عبر Maximal Marginal Relevance |

#### 4.6.4 إعدادات نماذج الاستدلال

| الإعداد | الوصف |
|---|---|
| `defaultModel` | النموذج الافتراضي للاستدلال |
| `temperature` | درجة الإبداعية (0.0 — 1.0) |
| `maxOutputTokens` | الحد الأقصى لرموز الإخراج |
| `systemPrompt` | تعليمات النظام المخصصة (System Prompt) |
| `responseLanguage` | لغة الرد: تلقائي / عربي / إنجليزي / مطابقة لغة السؤال |
| `citationStyle` | أسلوب المراجع: مضمّن / حواشي / مرقّم |

#### 4.6.5 إعدادات الخصوصية والأمان

| الإعداد | الوصف |
|---|---|
| `dataRetention` | مدة الاحتفاظ بالبيانات (30/60/90/∞ يوم) |
| `autoDeleteChats` | حذف المحادثات تلقائياً بعد فترة |
| `exportData` | تصدير جميع بياناتي (GDPR Compliance) |
| `deleteAccount` | حذف الحساب وجميع البيانات نهائياً |
| `apiKeys` | إدارة مفاتيح API الشخصية |
| `webhooks` | إعداد إشعارات Webhook للأحداث |

#### 4.6.6 إعدادات الويب والبحث الخارجي

| الإعداد | الوصف |
|---|---|
| `webSearchEnabled` | تفعيل/تعطيل البحث في الويب |
| `searchProvider` | مزود البحث: Google Search API / Bing / SearXNG |
| `searchScope` | نطاق البحث: عام / أكاديمي / أخبار |
| `searchResultsCount` | عدد نتائج الويب المُضمّنة |
| `privateDataPriority` | أولوية البيانات الخاصة على نتائج الويب (مُبدّل) |

---

### 📄 4.7 صفحة التحليلات والمراقبة (`/analytics`)

| المكوّن | الوصف |
|---|---|
| `QueryAnalytics` | تحليل الاستعلامات: الأكثر شيوعاً، أوقات الذروة، اللغات |
| `RetrievalQuality` | مقاييس جودة الاسترجاع: Recall@K, MRR, NDCG |
| `LatencyMonitor` | مراقبة زمن الاستجابة لكل مرحلة من مراحل الخط الأنابيب |
| `CostTracker` | متتبع التكاليف: استهلاك API لكل نموذج |
| `FeedbackDashboard` | لوحة ملاحظات المستخدمين (👍/👎) مع إحصائيات |
| `FailureLog` | سجل العمليات الفاشلة مع أسباب الفشل |

---

### 📄 4.8 صفحة API والتكاملات (`/api-docs`)

| المكوّن | الوصف |
|---|---|
| `APIPlayground` | ملعب تفاعلي لاختبار نقاط النهاية |
| `APIKeyManager` | إنشاء وإدارة مفاتيح API مع صلاحيات محددة |
| `WebhookConfig` | إعداد إشعارات Webhook لأحداث المعالجة |
| `SDKDocs` | وثائق SDK لـ JavaScript/Python |
| `EmbedWidget` | أداة تضمين ويدجت المحادثة في مواقع خارجية |

---

## 5. خط أنابيب الاستيعاب (Ingestion Pipeline) — التفصيل الكامل

```
[مصدر البيانات] → [الاستيعاب] → [المعالجة] → [التقسيم] → [التضمين] → [التخزين]

                         التفصيل خطوة بخطوة:

     ┌─────────────────────────────────────────────────────┐
 1.  │  📥 الاستيعاب (Ingestion)                            │
     │  • استقبال الملف/الرابط/البيانات                     │
     │  • التحقق من النوع والحجم والصلاحية                 │
     │  • تخزين الملف الأصلي في Blob Storage               │
     │  • إنشاء سجل في Neon Postgres مع tenant_id          │
     └──────────────────────┬──────────────────────────────┘
                            ▼
     ┌─────────────────────────────────────────────────────┐
 2.  │  🔄 المعالجة الأولية (Pre-processing)                │
     │  • كشف اللغة (عربي/إنجليزي/مختلط)                  │
     │  • كشف نوع المحتوى (نص/جدول/صورة/معادلة)           │
     │  • اختيار محرك المعالجة:                            │
     │    ├─ Mistral Doc AI → PDF معقد، ممسوح ضوئياً       │
     │    ├─ Unstructured → أنواع ملفات متنوعة             │
     │    └─ مُعالج داخلي → نص عادي، Markdown              │
     └──────────────────────┬──────────────────────────────┘
                            ▼
     ┌─────────────────────────────────────────────────────┐
 3.  │  ✂️ التقسيم الذكي (Smart Chunking)                   │
     │  • تقسيم حسب الاستراتيجية المختارة                  │
     │  • الحفاظ على السياق بين الأجزاء (التداخل)          │
     │  • إضافة بيانات وصفية لكل جزء:                     │
     │    {document_id, chunk_index, page_number,          │
     │     language, source_type, tenant_id,               │
     │     section_title, token_count}                     │
     │  • معالجة خاصة للمحتوى العربي:                     │
     │    ├─ تطبيع النص (Normalization)                    │
     │    ├─ إزالة التشكيل (اختياري)                       │
     │    └─ معالجة النصوص المختلطة (عربي+إنجليزي)         │
     └──────────────────────┬──────────────────────────────┘
                            ▼
     ┌─────────────────────────────────────────────────────┐
 4.  │  🧮 التضمين (Embedding)                              │
     │  • gemini-embedding-2 (3072-dim vectors)            │
     │  • Task Prefix لتحسين الجودة:                       │
     │    ├─ "retrieval_document:" للمستندات                │
     │    └─ "retrieval_query:" للاستعلامات                 │
     │  • Batch Processing للكفاءة                         │
     │  • التعامل مع المحتوى متعدد الوسائط مباشرةً         │
     └──────────────────────┬──────────────────────────────┘
                            ▼
     ┌─────────────────────────────────────────────────────┐
 5.  │  💾 التخزين المزدوج (Dual Storage)                    │
     │  • Qdrant: المتجهات + Payload metadata               │
     │    └─ فلترة بـ tenant_id إلزامية                     │
     │  • Neon Postgres: النص الكامل + بيانات وصفية         │
     │    └─ فهرس GIN لـ Full-Text Search (عربي+إنجليزي)   │
     │    └─ pgvector كاحتياطي للبحث المتجهي               │
     │  • تحديث حالة المستند → "مُفهرس"                    │
     └─────────────────────────────────────────────────────┘
```

---

## 6. خط أنابيب الاستعلام الهجين (Hybrid Query Pipeline)

```
[استعلام المستخدم]
        │
        ▼
┌───────────────────┐
│ 1. تحليل الاستعلام │ ← كشف اللغة + كشف النية + استخراج الكيانات
└────────┬──────────┘
         ▼
┌───────────────────┐
│ 2. توسيع الاستعلام │ ← HyDE (Hypothetical Document Embeddings)
│    Query Expansion │   + ترجمة ثنائية للبحث عبر اللغات
└────────┬──────────┘
         ▼
    ┌────┴────┐
    ▼         ▼
┌────────┐ ┌────────┐
│ بحث    │ │ بحث    │
│ دلالي  │ │ معجمي  │   ← يعملان بالتوازي (Parallel)
│(Qdrant)│ │(Neon   │
│        │ │ FTS)   │
└───┬────┘ └───┬────┘
    │          │
    ▼          ▼
┌───────────────────┐
│ 3. دمج النتائج    │ ← Reciprocal Rank Fusion (RRF)
│    Rank Fusion    │
└────────┬──────────┘
         ▼
┌───────────────────┐
│ 4. إعادة الترتيب  │ ← Cross-Encoder Re-ranking (اختياري)
│    Re-ranking     │
└────────┬──────────┘
         ▼
    ┌────┴────────────────────┐
    ▼                         ▼
┌──────────┐          ┌──────────────┐
│ وضع مقيّد │          │ وضع هجين     │
│ Private  │          │ Hybrid       │
│          │          │              │
│ النتائج  │          │ النتائج +    │
│ المحلية  │          │ بحث الويب    │
│ فقط      │          │ (مُرجّحة)    │
└────┬─────┘          └──────┬───────┘
     │                       │
     └───────┬───────────────┘
             ▼
┌───────────────────────┐
│ 5. تجميع السياق       │ ← Context Window Management
│    Context Assembly   │   مع ترتيب بحسب الصلة
└────────┬──────────────┘
         ▼
┌───────────────────────┐
│ 6. التوليد (LLM)      │ ← gemini-3.5-flash-lite أو gemini-3.6-flash
│    Generation         │   مع System Prompt + Retrieved Context
│                       │   + Streaming Response
└────────┬──────────────┘
         ▼
┌───────────────────────┐
│ 7. ما بعد التوليد     │ ← إرفاق المراجع + درجة الثقة
│    Post-processing    │   + اقتراحات متابعة + تسجيل
└───────────────────────┘
```

---

## 7. نظام العزل والخصوصية (Tenant Isolation & Privacy)

### 7.1 عزل البيانات (Data Isolation)

```
┌──────────────────────────────────────────────────────────┐
│                🔐 نموذج العزل متعدد الطبقات               │
│                                                          │
│  الطبقة 1: المصادقة (Authentication)                     │
│  ├─ JWT مُشفر مع tenant_id مضمّن                         │
│  ├─ تحقق في كل طلب API                                  │
│  └─ انتهاء صلاحية تلقائي + تجديد                         │
│                                                          │
│  الطبقة 2: قاعدة بيانات Postgres (RLS)                   │
│  ├─ Row-Level Security مُفعّل على كل الجداول             │
│  ├─ SET app.current_tenant = :tenant_id في كل جلسة       │
│  └─ لا يمكن الوصول لبيانات مستخدم آخر أبداً             │
│                                                          │
│  الطبقة 3: Qdrant (Payload Filtering)                    │
│  ├─ كل نقطة (Point) تحمل tenant_id في payload            │
│  ├─ فلترة إلزامية في كل استعلام بحث                      │
│  └─ Collections مشتركة مع عزل منطقي                     │
│                                                          │
│  الطبقة 4: تخزين الملفات (File Storage)                  │
│  ├─ مسارات معزولة: /{tenant_id}/files/*                  │
│  ├─ Signed URLs مع صلاحية مؤقتة                         │
│  └─ تشفير أثناء الراحة (At-Rest Encryption)              │
│                                                          │
│  الطبقة 5: طبقة API                                     │
│  ├─ Middleware يُلزم tenant_id في كل عملية               │
│  ├─ Rate Limiting لكل مستخدم                             │
│  └─ تسجيل شامل لكل عملية وصول                           │
└──────────────────────────────────────────────────────────┘
```

### 7.2 نموذج البيانات (Data Model — Neon Postgres)

```sql
-- جدول المستخدمين
CREATE TABLE users (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    tenant_id UUID NOT NULL UNIQUE,
    email TEXT NOT NULL UNIQUE,
    name TEXT,
    preferred_language TEXT DEFAULT 'ar', -- ar | en
    settings JSONB DEFAULT '{}',
    created_at TIMESTAMPTZ DEFAULT NOW()
);

-- جدول المصادر
CREATE TABLE sources (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    tenant_id UUID NOT NULL REFERENCES users(tenant_id),
    type TEXT NOT NULL, -- file | url | api | integration
    name TEXT NOT NULL,
    config JSONB, -- تكوين خاص بنوع المصدر
    sync_schedule TEXT, -- cron expression
    last_synced_at TIMESTAMPTZ,
    status TEXT DEFAULT 'active',
    created_at TIMESTAMPTZ DEFAULT NOW()
);

-- جدول المستندات
CREATE TABLE documents (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    tenant_id UUID NOT NULL REFERENCES users(tenant_id),
    source_id UUID REFERENCES sources(id),
    title TEXT,
    content TEXT, -- النص الكامل المستخرج
    content_tsv TSVECTOR, -- فهرس البحث النصي الكامل
    language TEXT, -- ar | en | mixed
    metadata JSONB,
    processing_engine TEXT, -- mistral | unstructured | internal
    status TEXT DEFAULT 'pending', -- pending | processing | indexed | failed
    file_path TEXT,
    page_count INTEGER,
    token_count INTEGER,
    created_at TIMESTAMPTZ DEFAULT NOW()
);

-- جدول الأجزاء (Chunks)
CREATE TABLE chunks (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    tenant_id UUID NOT NULL REFERENCES users(tenant_id),
    document_id UUID NOT NULL REFERENCES documents(id),
    content TEXT NOT NULL,
    content_tsv TSVECTOR,
    chunk_index INTEGER NOT NULL,
    page_number INTEGER,
    section_title TEXT,
    language TEXT,
    token_count INTEGER,
    embedding_id TEXT, -- معرف النقطة في Qdrant
    metadata JSONB,
    created_at TIMESTAMPTZ DEFAULT NOW()
);

-- جدول المحادثات
CREATE TABLE conversations (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    tenant_id UUID NOT NULL REFERENCES users(tenant_id),
    title TEXT,
    mode TEXT DEFAULT 'private', -- private | hybrid | general | analysis
    model TEXT DEFAULT 'gemini-3.6-flash',
    collection_ids UUID[], -- المجموعات المستهدفة
    created_at TIMESTAMPTZ DEFAULT NOW(),
    updated_at TIMESTAMPTZ DEFAULT NOW()
);

-- جدول الرسائل
CREATE TABLE messages (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    tenant_id UUID NOT NULL REFERENCES users(tenant_id),
    conversation_id UUID NOT NULL REFERENCES conversations(id),
    role TEXT NOT NULL, -- user | assistant | system
    content TEXT NOT NULL,
    citations JSONB, -- [{chunk_id, score, snippet}]
    model_used TEXT,
    tokens_used JSONB, -- {input: n, output: n}
    feedback TEXT, -- positive | negative | null
    created_at TIMESTAMPTZ DEFAULT NOW()
);

-- تفعيل RLS على كل الجداول
ALTER TABLE sources ENABLE ROW LEVEL SECURITY;
ALTER TABLE documents ENABLE ROW LEVEL SECURITY;
ALTER TABLE chunks ENABLE ROW LEVEL SECURITY;
ALTER TABLE conversations ENABLE ROW LEVEL SECURITY;
ALTER TABLE messages ENABLE ROW LEVEL SECURITY;

-- سياسة RLS موحدة
CREATE POLICY tenant_isolation ON sources
    USING (tenant_id = current_setting('app.current_tenant')::UUID);
-- (تُكرر لكل جدول)

-- فهرس البحث النصي الكامل للعربية والإنجليزية
CREATE INDEX idx_chunks_fts ON chunks USING GIN(content_tsv);
CREATE INDEX idx_documents_fts ON documents USING GIN(content_tsv);
```

---

## 8. دعم اللغة العربية — معالجة متقدمة

### 8.1 معالجة النص العربي

```
┌──────────────────────────────────────────────────────┐
│              🌍 خط أنابيب المعالجة العربية             │
│                                                      │
│  1. التطبيع (Normalization)                           │
│     ├─ توحيد الهمزات (أ/إ/آ → ا)                     │
│     ├─ توحيد التاء المربوطة/المفتوحة                 │
│     ├─ إزالة التشكيل (اختياري قابل للتعديل)          │
│     ├─ توحيد الأرقام (عربية/هندية)                   │
│     └─ تنظيف الرموز غير الضرورية                     │
│                                                      │
│  2. كشف اللغة (Language Detection)                   │
│     ├─ كشف تلقائي على مستوى الفقرة                   │
│     ├─ دعم النص المختلط (عربي+إنجليزي)               │
│     └─ تعيين علامة اللغة لكل جزء                     │
│                                                      │
│  3. تقسيم ذكي للعربية (Arabic-Aware Chunking)        │
│     ├─ مراعاة اتجاه النص (RTL)                       │
│     ├─ عدم قطع الجمل العربية في منتصفها              │
│     ├─ التعامل مع الاقتباسات والأقواس العربية        │
│     └─ الحفاظ على السياق في النصوص الدينية/الأدبية   │
│                                                      │
│  4. البحث عبر اللغات (Cross-Lingual Search)          │
│     ├─ توسيع الاستعلام بالترجمة التلقائية            │
│     ├─ بحث موحد عبر المحتوى العربي والإنجليزي        │
│     └─ gemini-embedding-2 يدعم 100+ لغة أصلاً        │
└──────────────────────────────────────────────────────┘
```

يدعم نموذج gemini-embedding-2 التضمين عبر أكثر من 100 لغة مما يتيح البحث والتصنيف والتجميع عبر اللغات بشكل أصلي، مما يُعد ركيزة أساسية للدعم العربي/الإنجليزي في النظام.

---

## 9. تفصيل استخدام النماذج (Model Usage Strategy)

### 9.1 مصفوفة استخدام النماذج

| السيناريو | النموذج | السبب |
|---|---|---|
| **تضمين المستندات** | `gemini-embedding-2` | الأفضل للبحث الدلالي عبر الوسائط، واسترجاع المستندات، وأنظمة التوصية التي تتطلب حسابات تشابه سريعة وقابلة للتوسع |
| **تضمين الاستعلامات** | `gemini-embedding-2` | يدعم تعليمات مهام مخصصة مثل task:code retrieval لتحسين التضمينات للعلاقات المستهدفة |
| **معالجة سريعة بتكلفة منخفضة** | `gemini-3.5-flash-lite` | محسّن للمهام البرمجية البسيطة وفهم المستندات الدقيق وسير العمل الوكيلي الخفيف الذي يتطلب استدلالاً سريعاً بأقل تكلفة |
| **استدلال معقد وتحليل عميق** | `gemini-3.6-flash` | يحتفظ بنافذة سياق المليون رمز ويستخدم حوالي 17% أقل من رموز الإخراج مقارنة بـ 3.5 Flash مع درجات أعلى في معايير البرمجة والسياق الطويل |
| **تصنيف واستخراج** | `gemini-3.5-flash-lite` | يُفضل مستوى التفكير الأدنى للمهام الحساسة للتأخير أو مهام التصنيف والاستخراج البسيطة لتحقيق أقصى كفاءة |
| **توليد إجابات RAG عادية** | `gemini-3.6-flash` | نموذج العمل اليومي لأعمال البرمجة والمعرفة والمدخلات متعددة الوسائط وسير العمل الوكيلي |
| **أسئلة متابعة وتلخيص** | `gemini-3.5-flash-lite` | للمهام الأخف التي لا تتطلب استدلالاً عميقاً |

### 9.2 التوجيه الذكي للنماذج (Smart Model Routing)

```
┌─────────────────────────────────────────────┐
│         🔀 محرك التوجيه الذكي               │
│                                             │
│  الاستعلام الوارد                           │
│       │                                     │
│       ▼                                     │
│  [تحليل التعقيد]                            │
│       │                                     │
│  ┌────┼─────────────────────────────────┐   │
│  │    ▼                                 │   │
│  │  تعقيد منخفض:                       │   │
│  │  • أسئلة مباشرة                     │   │
│  │  • تلخيص قصير                       │   │
│  │  • استخراج معلومة                    │   │
│  │  → gemini-3.5-flash-lite             │   │
│  │                                      │   │
│  │  تعقيد عالٍ:                         │   │
│  │  • تحليل مقارن                       │   │
│  │  • استدلال متعدد الخطوات             │   │
│  │  • تلخيص شامل لمستندات متعددة        │   │
│  │  • أسئلة تتطلب سياقاً طويلاً         │   │
│  │  → gemini-3.6-flash                  │   │
│  └──────────────────────────────────────┘   │
│                                             │
│  * يمكن للمستخدم تجاوز التوجيه يدوياً      │
└─────────────────────────────────────────────┘
```

---

## 10. واجهات API الرئيسية (API Endpoints)

```
📡 API Routes (/api/v1/)

Auth:
  POST   /auth/register          → تسجيل مستخدم جديد
  POST   /auth/login             → تسجيل الدخول
  POST   /auth/refresh           → تجديد الرمز

Sources:
  GET    /sources                → قائمة المصادر
  POST   /sources                → إضافة مصدر جديد
  PUT    /sources/:id            → تحديث مصدر
  DELETE /sources/:id            → حذف مصدر
  POST   /sources/:id/sync       → مزامنة فورية

Documents:
  GET    /documents              → قائمة المستندات
  POST   /documents/upload       → رفع مستند
  GET    /documents/:id          → تفاصيل مستند
  DELETE /documents/:id          → حذف مستند
  GET    /documents/:id/chunks   → أجزاء المستند

Knowledge:
  GET    /collections            → قائمة المجموعات
  POST   /collections            → إنشاء مجموعة
  PUT    /collections/:id        → تحديث مجموعة
  DELETE /collections/:id        → حذف مجموعة

Chat:
  GET    /conversations          → قائمة المحادثات
  POST   /conversations          → محادثة جديدة
  POST   /chat/completions       → إرسال رسالة (Streaming SSE)
  GET    /conversations/:id/messages → رسائل محادثة

Search:
  POST   /search                 → بحث هجين في قاعدة المعرفة
  POST   /search/semantic        → بحث دلالي فقط
  POST   /search/lexical         → بحث معجمي فقط

Analytics:
  GET    /analytics/usage        → إحصائيات الاستخدام
  GET    /analytics/quality      → مقاييس الجودة

Settings:
  GET    /settings               → إعدادات المستخدم
  PUT    /settings               → تحديث الإعدادات
  POST   /settings/export        → تصدير البيانات
  DELETE /settings/account       → حذف الحساب
```

---

## 11. ميزات متقدمة (Advanced Features)

### 11.1 البحث الهجين مع الويب (Hybrid Web Search)

```
┌─────────────────────────────────────────────────┐
│  🌐 وضع البحث الهجين (Private Data + Web)       │
│                                                 │
│  1. استعلام المستخدم يُحلل أولاً               │
│  2. يُبحث في البيانات الخاصة (RAG محلي)         │
│  3. يُستخرج السياق والفجوات المعرفية            │
│  4. تُصاغ استعلامات بحث ويب ذكية               │
│     بناءً على الفجوات                           │
│  5. تُجلب نتائج الويب وتُرشّح                  │
│  6. تُدمج النتائج المحلية + الويب               │
│     مع أولوية للبيانات الخاصة                   │
│  7. يُولّد الرد مع تمييز واضح:                  │
│     📁 من بياناتك الخاصة                        │
│     🌐 من الويب                                  │
│  8. المراجع مُرفقة ومُصنفة حسب المصدر           │
└─────────────────────────────────────────────────┘
```

### 11.2 الميزات المتقدمة الأخرى

| الميزة | الوصف |
|---|---|
| **Agentic RAG** | وكيل ذكي يقرر متى يبحث محلياً أو في الويب أو يطلب توضيحاً |
| **Multi-Collection Query** | استعلام عبر مجموعات معرفية متعددة بقرار واحد |
| **Conversational Memory** | ذاكرة محادثة قصيرة (Chat History) وطويلة (Summary Memory) |
| **Auto-Tagging** | تصنيف تلقائي للمستندات المُستوعبة باستخدام LLM |
| **Duplicate Detection** | كشف المستندات المكررة عبر التشابه التضميني |
| **Scheduled Sync** | مزامنة دورية تلقائية للمصادر الخارجية |
| **Feedback Loop** | تعلم من ملاحظات المستخدم لتحسين الاسترجاع |
| **Citation Verification** | التحقق من أن كل ادعاء في الإجابة مدعوم بمصدر |

---

## 12. الأداء والتحسين (Performance & Optimization)

| الجانب | الاستراتيجية |
|---|---|
| **تخزين مؤقت** | تخزين نتائج الاستعلامات الشائعة + تخزين التضمينات المحسوبة |
| **Streaming** | Server-Sent Events (SSE) للتدفق المباشر للإجابات |
| **Edge Computing** | Vercel Edge Functions لتقليل زمن الاستجابة عالمياً |
| **Batch Processing** | يعالج النموذج حتى 8,192 رمز نصي و6 صور و120 ثانية فيديو و180 ثانية صوت و6 صفحات PDF في طلب واحد |
| **Connection Pooling** | مجمّع اتصالات Neon Postgres عبر `@neondatabase/serverless` |
| **Lazy Loading** | تحميل المكوّنات عند الحاجة مع React Suspense |
| **Optimistic Updates** | تحديثات متفائلة في واجهة المستخدم |

---

## 13. الأمان (Security)

| الطبقة | التدابير |
|---|---|
| **النقل** | HTTPS/TLS 1.3 إلزامي |
| **المصادقة** | JWT + Refresh Tokens + MFA اختياري |
| **التفويض** | RBAC + Row-Level Security |
| **البيانات** | تشفير AES-256 أثناء الراحة، TLS أثناء النقل |
| **API** | Rate Limiting + API Key Scoping + CORS |
| **المدخلات** | تنظيف المدخلات + حماية من Prompt Injection |
| **التدقيق** | سجل تدقيق شامل (Audit Log) لكل عملية |
| **الامتثال** | دعم GDPR (تصدير/حذف البيانات) |

---

## 14. خارطة طريق النشر (Deployment)

```
┌──────────────────────────────────────────────┐
│            🚀 بنية النشر على Vercel           │
│                                              │
│  ┌──────────────────────────────────────┐    │
│  │  Vercel Edge Network                  │    │
│  │  ├─ Next.js App (SSR + API Routes)   │    │
│  │  ├─ Edge Functions (Auth, Routing)   │    │
│  │  ├─ Serverless Functions (Heavy ops) │    │
│  │  ├─ Vercel KV (Session Cache)        │    │
│  │  └─ Vercel Blob (File Storage)       │    │
│  └──────────────────────────────────────┘    │
│                    │                          │
│         ┌─────────┼──────────┐               │
│         ▼         ▼          ▼               │
│  ┌──────────┐ ┌────────┐ ┌────────┐         │
│  │  Neon    │ │ Qdrant │ │External│         │
│  │ Postgres │ │ Cloud  │ │  APIs  │         │
│  │(Serverless)│        │ │        │         │
│  └──────────┘ └────────┘ └────────┘         │
│                             │                │
│                    ┌────────┴────────┐       │
│                    ▼                 ▼       │
│              ┌──────────┐    ┌────────────┐  │
│              │ Mistral  │    │Unstructured│  │
│              │ Doc AI   │    │ Transform  │  │
│              │ API      │    │ API        │  │
│              └──────────┘    └────────────┘  │
│                                              │
│  البدائل: Railway, Fly.io, AWS Amplify      │
└──────────────────────────────────────────────┘
```

---

## 15. ملخص المزايا التنافسية

| الميزة | القيمة |
|---|---|
| 🌍 **ثنائي اللغة أصلاً** | دعم عميق للعربية والإنجليزية في كل طبقة من النظام |
| 🔒 **عزل كامل** | كل مستخدم في فقاعة بيانات منفصلة تماماً عبر 5 طبقات عزل |
| ⚡ **هجين ذكي** | جمع بين البحث الدلالي والمعجمي + الويب في نظام واحد |
| 🧠 **نماذج متطورة** | أحدث نماذج Google (Gemini 3.6) مع تضمين متعدد الوسائط |
| 📄 **معالجة مستندات مرنة** | خيار بين Mistral وUnstructured أو الاختيار التلقائي |
| 🔌 **تكاملات واسعة** | أكثر من 15 مصدر بيانات مدمج |
| 💰 **تحسين التكلفة** | توجيه ذكي للنماذج يُقلل التكاليف بنسبة تصل إلى 70% |
| 🚀 **Serverless بالكامل** | لا خوادم لإدارتها — يتوسع تلقائياً |

---

> **هذه الوثيقة تُمثل المواصفات الفنية الكاملة لتطبيق OmniRAG وتُستخدم كمرجع أساسي للتطوير والتنفيذ. كل مكوّن مصمم ليعمل بشكل مستقل ويتكامل مع باقي المنظومة وفق مبادئ Clean Architecture و Domain-Driven Design.**

# 🔌 إضافة طبقة MCP — وثيقة التوسعة الشاملة لـ OmniRAG

---

## ما هو MCP ولماذا هو ضروري لـ OmniRAG؟

البروتوكول MCP (Model Context Protocol) هو بروتوكول مفتوح يُقيّس طريقة توفير السياق لنماذج اللغة الكبيرة — فكّر فيه كـ USB-C لتطبيقات الذكاء الاصطناعي، يوفر طريقة موحدة لربط النماذج بمصادر البيانات والأدوات المختلفة.

منذ إطلاقه، استمر MCP في النمو بمعدل مذهل — حيث تجاوز كل من TypeScript SDK و Python SDK عتبة المليار تنزيل إجمالي، وأصبح البروتوكول ركيزة البيانات والتفاعلية لسير عمل الوكلاء الذكيين.

في 2026، يعمل MCP جنباً إلى جنب مع معيار ناشئ ثانٍ هو **A2A (Agent-to-Agent Protocol)** من Google. حيث يتحكم MCP في كيفية اتصال الوكلاء بالأدوات ومصادر البيانات، بينما يتحكم A2A في التواصل المباشر بين الوكلاء أنفسهم — والبروتوكولان متكاملان وليسا متنافسَين.

---

## أحدث مواصفة: MCP 2026-07-28

أحدث مواصفة MCP (2026-07-28) تجعل البروتوكول **عديم الحالة تماماً (Stateless)** على مستوى البروتوكول — لا مزيد من تعريف الجلسة (initialize handshake) ولا تثبيت العميل بمعرف Mcp-Session-Id.

خادم MCP البعيد الذي كان يحتاج سابقاً إلى جلسات لزجة (sticky sessions) ومخزن جلسات مشترك وفحص عميق للحزم عند البوابة، يمكنه الآن العمل خلف موازن تحميل round-robin عادي.

على صعيد TypeScript، يُشحن البروتوكول الجديد كحزمتين جديدتين: `@modelcontextprotocol/client` و`@modelcontextprotocol/server`، كلتاهما بالإصدار 2.0، بينما يستمر خط `@modelcontextprotocol/sdk` القديم لخوادم الإصدار 2025.

---

## 🏗️ البنية المعمارية الشاملة لطبقة MCP في OmniRAG

```
┌────────────────────────────────────────────────────────────────────────┐
│                    🌐 OmniRAG — طبقة MCP الشاملة                        │
│                                                                        │
│  ┌──────────────────────────────────────────────────────────────────┐  │
│  │                    MCP Gateway (بوابة موحدة)                      │  │
│  │                                                                  │  │
│  │  ┌────────────┐  ┌────────────┐  ┌───────────┐  ┌────────────┐  │  │
│  │  │  Auth &    │  │  Rate      │  │  Tool     │  │  Audit     │  │  │
│  │  │  OAuth 2.0 │  │  Limiting  │  │  Router   │  │  Logger    │  │  │
│  │  └────────────┘  └────────────┘  └───────────┘  └────────────┘  │  │
│  └──────────────────────────────────────────────────────────────────┘  │
│                               │                                        │
│              ┌────────────────┼────────────────────┐                  │
│              ▼                ▼                     ▼                  │
│  ┌──────────────────┐ ┌──────────────────┐ ┌──────────────────────┐   │
│  │  MCP Servers     │ │  MCP Servers     │ │  MCP Servers         │   │
│  │  (البيانات)      │ │  (الأدوات)       │ │  (الإجراءات)         │   │
│  │                  │ │                  │ │                      │   │
│  │ • Notion         │ │ • Web Search     │ │ • Email Send         │   │
│  │ • Google Drive   │ │ • Code Exec      │ │ • Slack Message      │   │
│  │ • GitHub         │ │ • Calculator     │ │ • Calendar Create    │   │
│  │ • Confluence     │ │ • File Convert   │ │ • Webhook Trigger    │   │
│  │ • Slack History  │ │ • Data Extract   │ │ • DB Write           │   │
│  │ • PostgreSQL     │ │ • Chart Gen      │ │ • API Call           │   │
│  │ • Qdrant         │ │ • PDF Generate   │ │ • Notification       │   │
│  └──────────────────┘ └──────────────────┘ └──────────────────────┘   │
│                               │                                        │
│              ┌────────────────┼────────────────────┐                  │
│              ▼                ▼                     ▼                  │
│  ┌──────────────────────────────────────────────────────────────────┐  │
│  │          RAG Agentic Engine (محرك الوكيل المدمج)                  │  │
│  │   MCP Tools ←→ LLM (Gemini) ←→ RAG Pipeline ←→ Hybrid Search   │  │
│  └──────────────────────────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────────────────────────┘
```

---

## 1. طبقة MCP Gateway — القلب التقني

### 1.1 الدور والمهام

بوابة MCP هي طبقة وسيطة مركزية لإدارة وتأمين وتوسيع نطاق الاتصالات بين تطبيقات الذكاء الاصطناعي وخوادم MCP المتعددة.

عندما يُرسل عميل طلب `tools/call`، تحدد البوابة خادم MCP المسؤول عن الأداة وتُعيد توجيه الطلب وفقاً لذلك، مما يدعم تنسيق متعدد الخوادم حيث يمكن لتطبيق ذكاء اصطناعي واحد الوصول إلى وظائف من خلفيات عديدة.

### 1.2 بنية الـ MCP Gateway في الكود (أحدث مواصفة 2026-07-28)

```typescript
// /app/api/mcp/[...path]/route.ts
// يستخدم أحدث SDK: @modelcontextprotocol/server v2.0

import { createMcpHandler, McpServer } from '@modelcontextprotocol/server';
import { toNodeHandler } from '@modelcontextprotocol/node';
import { z } from 'zod/v4';
import { verifyTenantFromRequest } from '@/lib/auth/tenant';
import { MCPToolRegistry } from '@/lib/mcp/registry';
import { MCPAuditLogger } from '@/lib/mcp/audit';

// ╔══════════════════════════════════════╗
// ║  MCP Gateway Handler (Stateless)     ║
// ║  متوافق مع مواصفة 2026-07-28        ║
// ╚══════════════════════════════════════╝

const mcpHandler = createMcpHandler((req) => {
  // كل طلب مستقل — بدون sessions (Stateless)
  // tenant_id يُمرر عبر معاملات الأداة مباشرةً
  
  const server = new McpServer(
    { name: 'omnirag-mcp-gateway', version: '2.0.0' },
    { 
      capabilities: { 
        tools: { listChanged: true },
        resources: { listChanged: true, subscribe: true },
        prompts: {}
      } 
    }
  );

  // تسجيل الأدوات ديناميكياً من سجل الأدوات
  MCPToolRegistry.registerAll(server);

  return server;
});

export { 
  mcpHandler as GET, 
  mcpHandler as POST, 
  mcpHandler as DELETE 
};
```

### 1.3 سجل الأدوات الديناميكي (Dynamic Tool Registry)

```typescript
// /lib/mcp/registry.ts
import { McpServer } from '@modelcontextprotocol/server';
import { z } from 'zod/v4';

// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
// كل أداة تتضمن:
//   • اسم وصفي واضح (Tool Naming SEP-986)
//   • مخطط مدخل/مخرج صارم مع Zod
//   • إفصاح عن الآثار الجانبية (Side Effects)
//   • tenant_id إلزامي في كل أداة للعزل
// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

export class MCPToolRegistry {
  static registerAll(server: McpServer) {
    // ═══ مجموعة أدوات البحث ═══
    this.registerSearchTools(server);
    // ═══ مجموعة أدوات جلب البيانات ═══
    this.registerDataFetchTools(server);
    // ═══ مجموعة أدوات التفاعل الخارجي ═══
    this.registerExternalActionTools(server);
    // ═══ مجموعة أدوات إدارة قاعدة المعرفة ═══
    this.registerKnowledgeTools(server);
  }

  // ─── أدوات البحث ──────────────────────────────────────
  private static registerSearchTools(server: McpServer) {
    
    // البحث الدلالي في قاعدة المعرفة الخاصة
    server.tool(
      'knowledge_semantic_search',
      'البحث الدلالي في قاعدة معرفة المستخدم باستخدام متجهات التضمين',
      {
        tenant_id: z.string().uuid(),
        query: z.string().min(1).max(2000),
        collection_ids: z.array(z.string().uuid()).optional(),
        top_k: z.number().int().min(1).max(20).default(5),
        score_threshold: z.number().min(0).max(1).default(0.7),
        language: z.enum(['ar', 'en', 'auto']).default('auto'),
      },
      async ({ tenant_id, query, collection_ids, top_k, score_threshold, language }) => {
        const results = await hybridSearch({
          tenantId: tenant_id,
          query,
          collectionIds: collection_ids,
          topK: top_k,
          scoreThreshold: score_threshold,
          language,
        });
        return {
          content: [{ 
            type: 'text', 
            text: JSON.stringify(results, null, 2) 
          }],
        };
      }
    );

    // البحث في الويب الحي
    server.tool(
      'web_live_search',
      'البحث في الإنترنت للحصول على معلومات حديثة ومحدّثة',
      {
        tenant_id: z.string().uuid(),
        query: z.string(),
        num_results: z.number().int().min(1).max(10).default(5),
        search_type: z.enum(['general', 'academic', 'news', 'images']).default('general'),
        language: z.enum(['ar', 'en', 'auto']).default('auto'),
        date_filter: z.enum(['day', 'week', 'month', 'year', 'any']).default('any'),
      },
      async (params) => { /* ... */ }
    );
  }

  // ─── أدوات جلب البيانات ────────────────────────────────
  private static registerDataFetchTools(server: McpServer) {
    
    // جلب محتوى URL
    server.tool(
      'fetch_url_content',
      'جلب محتوى صفحة ويب وتحويلها إلى Markdown نظيف',
      {
        tenant_id: z.string().uuid(),
        url: z.string().url(),
        include_links: z.boolean().default(false),
        include_images: z.boolean().default(false),
        extract_mode: z.enum(['full', 'main_content', 'metadata_only']).default('main_content'),
      },
      async (params) => { /* ... */ }
    );

    // جلب ملف من Google Drive
    server.tool(
      'google_drive_fetch_file',
      'جلب ملف من Google Drive وتحويله إلى نص قابل للمعالجة',
      {
        tenant_id: z.string().uuid(),
        file_id: z.string(),
        export_format: z.enum(['text', 'markdown', 'json']).default('markdown'),
      },
      async (params) => { /* ... */ }
    );

    // جلب صفحات Notion
    server.tool(
      'notion_fetch_page',
      'جلب محتوى صفحة أو قاعدة بيانات من Notion',
      {
        tenant_id: z.string().uuid(),
        page_id: z.string(),
        include_children: z.boolean().default(true),
        depth: z.number().int().min(1).max(5).default(2),
      },
      async (params) => { /* ... */ }
    );

    // جلب مستودع GitHub
    server.tool(
      'github_fetch_repository',
      'جلب محتوى مستودع GitHub: README, Issues, Wiki, الكود',
      {
        tenant_id: z.string().uuid(),
        owner: z.string(),
        repo: z.string(),
        content_types: z.array(
          z.enum(['readme', 'issues', 'wiki', 'code', 'discussions', 'releases'])
        ).default(['readme']),
        branch: z.string().default('main'),
        file_extensions: z.array(z.string()).optional(),
        max_files: z.number().int().max(100).default(20),
      },
      async (params) => { /* ... */ }
    );

    // قراءة رسائل Slack
    server.tool(
      'slack_read_messages',
      'قراءة رسائل من قناة Slack محددة للفهرسة',
      {
        tenant_id: z.string().uuid(),
        channel_id: z.string(),
        limit: z.number().int().min(1).max(1000).default(100),
        from_date: z.string().datetime().optional(),
        to_date: z.string().datetime().optional(),
        include_threads: z.boolean().default(true),
      },
      async (params) => { /* ... */ }
    );

    // استعلام قاعدة بيانات PostgreSQL خارجية
    server.tool(
      'external_postgres_query',
      'تنفيذ استعلام قراءة فقط على قاعدة بيانات PostgreSQL خارجية',
      {
        tenant_id: z.string().uuid(),
        connection_id: z.string().uuid(), // معرف الاتصال المحفوظ
        query: z.string().max(5000),
        parameters: z.array(z.any()).optional(),
        timeout_ms: z.number().int().default(30000),
      },
      async (params) => { /* ... */ }
    );

    // قراءة Confluence
    server.tool(
      'confluence_fetch_space',
      'جلب محتوى مساحة أو صفحات من Confluence',
      {
        tenant_id: z.string().uuid(),
        space_key: z.string(),
        page_ids: z.array(z.string()).optional(),
        include_children: z.boolean().default(true),
        expand_body: z.boolean().default(true),
      },
      async (params) => { /* ... */ }
    );
  }

  // ─── أدوات التفاعل الخارجي ────────────────────────────
  private static registerExternalActionTools(server: McpServer) {
    
    // إرسال رسالة Slack
    server.tool(
      'slack_send_message',
      '⚠️ [SIDE EFFECT] إرسال رسالة إلى قناة Slack',
      {
        tenant_id: z.string().uuid(),
        channel_id: z.string(),
        message: z.string().max(4000),
        thread_ts: z.string().optional(),
        blocks: z.array(z.any()).optional(), // Slack Block Kit
      },
      async (params) => { /* ... */ }
    );

    // إرسال بريد إلكتروني
    server.tool(
      'email_send',
      '⚠️ [SIDE EFFECT] إرسال بريد إلكتروني عبر الحساب المرتبط',
      {
        tenant_id: z.string().uuid(),
        to: z.array(z.string().email()),
        cc: z.array(z.string().email()).optional(),
        subject: z.string().max(200),
        body: z.string().max(50000),
        body_format: z.enum(['text', 'html', 'markdown']).default('markdown'),
        attachments: z.array(z.object({
          filename: z.string(),
          content_base64: z.string(),
        })).optional(),
      },
      async (params) => { /* ... */ }
    );

    // إنشاء حدث في التقويم
    server.tool(
      'calendar_create_event',
      '⚠️ [SIDE EFFECT] إنشاء حدث في Google Calendar أو Outlook',
      {
        tenant_id: z.string().uuid(),
        title: z.string(),
        description: z.string().optional(),
        start_datetime: z.string().datetime(),
        end_datetime: z.string().datetime(),
        attendees: z.array(z.string().email()).optional(),
        location: z.string().optional(),
        timezone: z.string().default('UTC'),
        calendar_provider: z.enum(['google', 'outlook', 'caldav']).default('google'),
      },
      async (params) => { /* ... */ }
    );

    // إنشاء Issue في GitHub
    server.tool(
      'github_create_issue',
      '⚠️ [SIDE EFFECT] إنشاء Issue جديد في مستودع GitHub',
      {
        tenant_id: z.string().uuid(),
        owner: z.string(),
        repo: z.string(),
        title: z.string(),
        body: z.string(),
        labels: z.array(z.string()).optional(),
        assignees: z.array(z.string()).optional(),
        milestone: z.number().int().optional(),
      },
      async (params) => { /* ... */ }
    );

    // إنشاء صفحة Notion
    server.tool(
      'notion_create_page',
      '⚠️ [SIDE EFFECT] إنشاء صفحة جديدة في Notion',
      {
        tenant_id: z.string().uuid(),
        parent_id: z.string(),
        parent_type: z.enum(['page', 'database']),
        title: z.string(),
        content_markdown: z.string(),
        properties: z.record(z.any()).optional(),
      },
      async (params) => { /* ... */ }
    );

    // تشغيل Webhook
    server.tool(
      'webhook_trigger',
      '⚠️ [SIDE EFFECT] إرسال طلب HTTP إلى Webhook خارجي',
      {
        tenant_id: z.string().uuid(),
        webhook_id: z.string().uuid(), // معرف Webhook محفوظ ومُعتمد
        payload: z.record(z.any()),
        method: z.enum(['POST', 'PUT', 'PATCH']).default('POST'),
      },
      async (params) => { /* ... */ }
    );

    // إنشاء تقرير PDF
    server.tool(
      'document_generate_report',
      '⚠️ [SIDE EFFECT] توليد تقرير PDF من البيانات وتخزينه',
      {
        tenant_id: z.string().uuid(),
        title: z.string(),
        sections: z.array(z.object({
          heading: z.string(),
          content: z.string(),
          chart_data: z.any().optional(),
        })),
        language: z.enum(['ar', 'en']).default('ar'),
        template: z.enum(['report', 'summary', 'analysis']).default('report'),
      },
      async (params) => { /* ... */ }
    );
  }

  // ─── أدوات قاعدة المعرفة ─────────────────────────────
  private static registerKnowledgeTools(server: McpServer) {
    
    // استيعاب مستند جديد
    server.tool(
      'knowledge_ingest_document',
      '⚠️ [SIDE EFFECT] استيعاب ومعالجة مستند وإضافته لقاعدة المعرفة',
      {
        tenant_id: z.string().uuid(),
        content: z.string(),
        metadata: z.object({
          title: z.string(),
          source_url: z.string().url().optional(),
          source_type: z.string(),
          language: z.enum(['ar', 'en', 'mixed']).optional(),
          tags: z.array(z.string()).optional(),
        }),
        collection_id: z.string().uuid().optional(),
        processing_engine: z.enum(['mistral', 'unstructured', 'auto']).default('auto'),
      },
      async (params) => { /* ... */ }
    );

    // استرجاع سجل المحادثة
    server.tool(
      'conversation_get_history',
      'استرجاع سجل محادثة سابقة للسياق',
      {
        tenant_id: z.string().uuid(),
        conversation_id: z.string().uuid(),
        last_n_messages: z.number().int().min(1).max(50).default(10),
      },
      async (params) => { /* ... */ }
    );
  }
}
```

---

## 2. المصادر والموارد (MCP Resources) — مكوّن منفصل عن الأدوات

في بروتوكول MCP: **الأدوات (Tools)** هي إجراءات قابلة للتنفيذ مع مخططات مدخل/مخرج صريحة وإفصاح عن الآثار الجانبية. **الموارد (Resources)** هي مصادر بيانات قابلة للقراءة يمكن للعميل جلبها لسياق النموذج. **التعليمات (Prompts)** هي قوالب تفاعل مُعرّفة مسبقاً تُقيّس المهام.

```typescript
// /lib/mcp/resources.ts
// تسجيل الموارد (Resources) — بيانات قابلة للقراءة

export function registerResources(server: McpServer, tenantId: string) {

  // ─── مورد: قائمة المجموعات
  server.resource(
    'knowledge_collections',
    `omnirag://collections/${tenantId}`,
    async (uri) => {
      const collections = await getCollections(tenantId);
      return {
        contents: [{
          uri: uri.href,
          mimeType: 'application/json',
          text: JSON.stringify(collections),
        }],
      };
    }
  );

  // ─── مورد ديناميكي: محتوى مستند بمعرفه
  server.resource(
    'document_content',
    new ResourceTemplate(
      'omnirag://documents/{tenant_id}/{document_id}', 
      { list: undefined }
    ),
    async (uri, params) => {
      const doc = await getDocument(params.tenant_id, params.document_id);
      return {
        contents: [{
          uri: uri.href,
          mimeType: 'text/plain',
          text: doc.content,
        }],
      };
    }
  );

  // ─── مورد: إحصائيات الاستخدام
  server.resource(
    'usage_stats',
    `omnirag://stats/${tenantId}`,
    async (uri) => {
      const stats = await getUsageStats(tenantId);
      return {
        contents: [{
          uri: uri.href,
          mimeType: 'application/json',
          text: JSON.stringify(stats),
        }],
      };
    }
  );
}
```

---

## 3. خوادم MCP الخارجية المُدمجة (External MCP Servers Catalogue)

يتكامل OmniRAG مع مجموعة واسعة من خوادم MCP الرسمية والمجتمعية:

يدعم Zapier MCP خادماً ديناميكياً يتصل بأي من أكثر من **8000 تطبيق** على منصة Zapier.

### 3.1 خوادم البيانات والمعرفة

| خادم MCP | النوع | الوصف | التكامل |
|---|---|---|---|
| **Notion MCP** | رسمي | يُجسّر Notion API للبحث في المحتوى واستعلام قواعد البيانات وإدارة الصفحات والتعليقات | OAuth 2.0 |
| **Google Drive MCP** | رسمي | جلب ومزامنة الملفات والمجلدات | OAuth 2.0 |
| **GitHub MCP** | رسمي | يسمح بأسئلة مثل "أرِني جميع Issues المفتوحة ذات تسمية bug التي أُنشئت في آخر 7 أيام" أو "أي الملفات تغيّرت أكثر في الشهر الماضي" | GitHub App |
| **Confluence MCP** | رسمي | جلب المساحات والصفحات والمحتوى | OAuth 2.0 |
| **Slack MCP** | رسمي | قراءة/إرسال الرسائل وإدارة القنوات | OAuth 2.0 |
| **PostgreSQL MCP** | رسمي | تحويل قاعدة البيانات العلائقية إلى خادم MCP لإجراء استعلامات معقدة ومنظمة على البيانات التقليدية | Connection String |
| **Qdrant MCP** | رسمي | قاعدة بيانات متجهية عالية الأداء تعمل كذاكرة ذكية للوكلاء وتستدعي البيانات الأكثر صلة دلالياً | API Key |
| **Apify MCP** | مجتمعي | أكثر من 3,000 أداة سحابية مُعدّة مسبقاً لاستخراج البيانات من المواقع والتجارة الإلكترونية ووسائل التواصل الاجتماعي ومحركات البحث والخرائط | API Key |
| **Google Calendar MCP** | مجتمعي | يُتيح للمساعدين التفكير في الجداول والتوافر وبيانات الاجتماعات بلغة طبيعية | OAuth 2.0 |
| **Firecrawl MCP** | رسمي | زحف الويب المتقدم واستخراج البيانات المنظمة | API Key |

### 3.2 خوادم الأدوات والإجراءات

| خادم MCP | النوع | الوصف |
|---|---|---|
| **Zapier MCP** | رسمي | خادم MCP ديناميكي يتصل بأي من 8000+ تطبيق على Zapier |
| **Fetch MCP** | مرجعي | جلب محتوى الويب وتحويله لاستخدام فعّال من قِبل النماذج |
| **Filesystem MCP** | مرجعي | عمليات ملفات آمنة مع ضوابط وصول قابلة للتهيئة |
| **Memory MCP** | مرجعي | نظام ذاكرة دائمة مبني على رسم بياني للمعرفة (Knowledge Graph) |
| **Sequential Thinking MCP** | مرجعي | حل المشكلات الديناميكي عبر تسلسل الأفكار |
| **n8n MCP** | مجتمعي | تكامل مع منصة أتمتة سير العمل n8n لتوفير وصول تحادثي لأكثر من 525 عقدة |
| **AWS MCP** | رسمي | أدوات للوصول إلى وثائق AWS والبحث في المحتوى والحصول على توصيات |
| **Telnyx MCP** | رسمي | تكامل مع APIs اتصالات Telnyx لمكالمات الصوت والرسائل SMS/MMS وإدارة أرقام الهاتف |

---

## 4. صفحة إدارة MCP في الواجهة (`/mcp-hub`)

### 4.1 هيكل الصفحة الكامل

```
┌──────────────────────────────────────────────────────────────────┐
│  🔌 مركز تكاملات MCP — OmniRAG                                    │
├──────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │  📊 لوحة الحالة السريعة                                   │    │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐   │    │
│  │  │ 12       │ │ 8        │ │ 1,247    │ │ 99.2%    │   │    │
│  │  │ خادم     │ │ متصل     │ │ استدعاء  │ │ نجاح     │   │    │
│  │  │ مُضاف    │ │ الآن     │ │ اليوم    │ │          │   │    │
│  │  └──────────┘ └──────────┘ └──────────┘ └──────────┘   │    │
│  └─────────────────────────────────────────────────────────┘    │
│                                                                  │
│  ┌──────────────────┐  ┌───────────────────────────────────┐   │
│  │  🗂️ التصنيفات    │  │  بطاقات الخوادم                     │   │
│  │                  │  │                                    │   │
│  │  ○ الكل (12)     │  │  ┌─────────────────────────────┐  │   │
│  │  ○ بيانات (5)    │  │  │ 📓 Notion              ✅     │  │   │
│  │  ○ أدوات (4)     │  │  │ آخر استدعاء: منذ 5 دقائق    │  │   │
│  │  ○ إجراءات (3)   │  │  │ استدعاءات اليوم: 47         │  │   │
│  │                  │  │  │ [إدارة] [اختبار] [تعطيل]    │  │   │
│  │  🔍 بحث          │  │  └─────────────────────────────┘  │   │
│  │  [___________]   │  │                                    │   │
│  │                  │  │  ┌─────────────────────────────┐  │   │
│  │  ➕ إضافة خادم   │  │  │ 🐙 GitHub              ✅     │  │   │
│  │                  │  │  │ آخر استدعاء: منذ 12 دقيقة  │  │   │
│  └──────────────────┘  │  │ استدعاءات اليوم: 23         │  │   │
│                         │  │ [إدارة] [اختبار] [تعطيل]    │  │   │
│                         │  └─────────────────────────────┘  │   │
│                         └───────────────────────────────────┘   │
└──────────────────────────────────────────────────────────────────┘
```

### 4.2 المكوّنات التفصيلية

| المكوّن | الوصف التقني |
|---|---|
| `MCPServerCard` | بطاقة كل خادم: الاسم، الأيقونة، الحالة، آخر استدعاء، عدد الاستدعاءات، أزرار الإدارة |
| `MCPServerWizard` | معالج إضافة خادم جديد: اختيار النوع → مصادقة OAuth/API → اختبار الاتصال → تفعيل |
| `MCPOAuthFlow` | تدفق OAuth 2.0 متكامل مع Resource Indicators (RFC 8707) للمواصفة الجديدة |
| `MCPToolExplorer` | مستعرض الأدوات المتاحة لكل خادم مع مخططات المدخلات/المخرجات |
| `MCPConnectionTester` | أداة اختبار الاتصال مع خادم MCP مع عرض الاستجابة الخام |
| `MCPCallLog` | سجل كامل لجميع استدعاءات الأدوات: الوقت، الخادم، الأداة، المدخلات، النتيجة، الزمن |
| `MCPPermissionManager` | إدارة صلاحيات كل أداة: قراءة فقط / قراءة وكتابة / تعطيل |
| `MCPHealthMonitor` | مراقبة صحة الاتصال في الوقت الفعلي مع تنبيهات الفشل |
| `MCPRateLimitConfig` | ضبط حد الاستدعاءات لكل خادم: أقصى عدد في الدقيقة/الساعة/اليوم |
| `MCPSecretVault` | تخزين مؤمّن لمفاتيح API والرموز الخاصة بكل خادم (مُشفّر بـ AES-256) |
| `MCPDiscoveryPanel` | استعراض خوادم MCP المتاحة من سجل MCP الرسمي وإضافتها بنقرة واحدة |

---

## 5. نظام المصادقة لخوادم MCP (OAuth 2.0 — RFC 8707)

تُصنّف مواصفة MCP يونيو 2025 خوادم MCP رسمياً كـ **OAuth Resource Servers**، وتُلزم عملاء MCP بتطبيق **Resource Indicators** وفق RFC 8707، مع صفحة مخصصة لأفضل ممارسات الأمان.

يجب على العملاء الآن التحقق من معامل `iss` في استجابات التفويض وفق RFC 9207، وهو تخفيف منخفض التكلفة لفئة من هجمات الخلط (mix-up attacks) الأكثر شيوعاً في نمط نشر MCP ذو العميل الواحد والخوادم المتعددة.

```typescript
// /lib/mcp/auth/oauth-manager.ts
// متوافق مع مواصفة 2026-07-28 — RFC 8707 + RFC 9207

import { 
  OAuthClientProvider,
  ResourceIndicatorValidator 
} from '@modelcontextprotocol/server';

export class MCPOAuthManager {

  // بدء تدفق OAuth مع Resource Indicators
  async initiateOAuthFlow(params: {
    tenantId: string;
    serverId: string;
    serverResourceUri: string; // RFC 8707: Resource Indicator
    scopes: string[];
    callbackUrl: string;
  }) {
    // التحقق من Resource Indicator لمنع Token Misuse
    const validated = await ResourceIndicatorValidator.validate({
      resourceUri: params.serverResourceUri,
      requestedScopes: params.scopes,
    });

    // إنشاء PKCE challenge (إلزامي)
    const { codeVerifier, codeChallenge } = await generatePKCE();

    // حفظ الحالة مع tenant isolation
    await this.storeOAuthState({
      tenantId: params.tenantId,
      serverId: params.serverId,
      codeVerifier,
      resourceUri: params.serverResourceUri,
      nonce: crypto.randomUUID(),
    });

    return buildAuthorizationUrl({
      ...validated,
      codeChallenge,
      state: await encryptState(params.tenantId, params.serverId),
    });
  }

  // معالجة callback مع التحقق من iss (RFC 9207)
  async handleOAuthCallback(params: {
    code: string;
    state: string;
    iss: string; // إلزامي في المواصفة 2026-07-28
  }) {
    // التحقق من iss لمنع هجمات الخلط
    const { tenantId, serverId } = await decryptState(params.state);
    const storedState = await this.getOAuthState(tenantId, serverId);

    // RFC 9207: التحقق من أن iss يطابق Authorization Server المتوقع
    await validateIssuerParameter({
      receivedIss: params.iss,
      expectedIss: storedState.expectedIssuer,
    });

    const tokens = await exchangeCodeForTokens({
      code: params.code,
      codeVerifier: storedState.codeVerifier,
      resourceIndicator: storedState.resourceUri,
    });

    // تخزين الرمز مشفراً مع tenant isolation
    await this.storeTokensSecurely(tenantId, serverId, tokens);
  }

  // تجديد الرمز تلقائياً مع Resource Binding
  async refreshTokenWithResourceBinding(tenantId: string, serverId: string) {
    const tokens = await this.getTokens(tenantId, serverId);
    const config = await this.getServerConfig(serverId);

    const newTokens = await refreshAccessToken({
      refreshToken: tokens.refreshToken,
      resource: config.resourceUri, // RFC 8707
    });

    await this.storeTokensSecurely(tenantId, serverId, newTokens);
    return newTokens;
  }
}
```

---

## 6. محرك الوكيل المدمج — Agentic RAG + MCP

### 6.1 التفكير التسلسلي (Chain-of-Thought + Tool Use)

```
┌─────────────────────────────────────────────────────────────────┐
│               🧠 دورة الوكيل الذكي (Agent Loop)                  │
│                                                                 │
│  استعلام المستخدم                                               │
│       │                                                         │
│       ▼                                                         │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  1. تخطيط المهمة (Task Planner)                           │  │
│  │     • تحليل الاستعلام وتحديد المعلومات المطلوبة          │  │
│  │     • تحديد الأدوات اللازمة من سجل MCP                  │  │
│  │     • بناء خطة تنفيذ خطوة بخطوة                         │  │
│  └───────────────────┬──────────────────────────────────────┘  │
│                       │                                         │
│       ┌───────────────┼────────────────────┐                   │
│       ▼               ▼                    ▼                   │
│  ┌─────────┐    ┌──────────┐         ┌──────────┐             │
│  │ RAG     │    │ MCP Tool │         │ Web      │             │
│  │ Search  │    │ Call 1   │         │ Search   │             │
│  │ (محلي)  │    │(Notion)  │         │ (حي)     │             │
│  └────┬────┘    └────┬─────┘         └────┬─────┘             │
│       │              │                    │                    │
│       └──────────────┼────────────────────┘                   │
│                       ▼                                         │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  2. دمج النتائج (Context Assembly)                        │  │
│  │     • دمج نتائج RAG + MCP + الويب                        │  │
│  │     • إزالة التكرار                                       │  │
│  │     • ترتيب بحسب الصلة والموثوقية                         │  │
│  └───────────────────┬──────────────────────────────────────┘  │
│                       ▼                                         │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  3. هل يحتاج الوكيل إلى أدوات إضافية؟                    │  │
│  │     ├─ نعم → تكرار الحلقة (Max 5 iterations)             │  │
│  │     └─ لا  → الانتقال للتوليد                             │  │
│  └───────────────────┬──────────────────────────────────────┘  │
│                       ▼                                         │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  4. التوليد النهائي (gemini-3.6-flash)                    │  │
│  │     • System Prompt + Context + Chat History             │  │
│  │     • Streaming Response + Citations                     │  │
│  │     • تمييز مصادر: 📁 محلي / 🔌 MCP / 🌐 ويب            │  │
│  └──────────────────────────────────────────────────────────┘  │
│                       │                                         │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  5. إجراءات ما بعد التوليد (Post-Generation Actions)      │  │
│  │     • هل الإجابة تستدعي إجراءً خارجياً؟                  │  │
│  │     • مثال: "حدّث سجل Notion بالملخص"                    │  │
│  │     • تأكيد من المستخدم قبل أي إجراء (Side Effect)       │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

### 6.2 كود محرك الوكيل

```typescript
// /lib/rag/agentic-engine.ts
import { google } from '@ai-sdk/google';
import { generateText, tool } from 'ai'; // Vercel AI SDK
import { z } from 'zod/v4';
import { MCPClientPool } from '@/lib/mcp/client-pool';

export class AgenticRAGEngine {

  async execute(params: {
    tenantId: string;
    conversationId: string;
    userMessage: string;
    mode: 'private' | 'hybrid' | 'agentic';
    model: 'gemini-3.5-flash-lite' | 'gemini-3.6-flash';
    enabledMCPServers: string[];
    maxIterations?: number;
  }) {
    const { tenantId, userMessage, model, maxIterations = 5 } = params;

    // جمع أدوات MCP النشطة للمستخدم
    const mcpTools = await MCPClientPool
      .getActiveTools(tenantId, params.enabledMCPServers);

    // أدوات RAG الداخلية
    const ragTools = this.buildRAGTools(tenantId);

    const result = await generateText({
      model: google(model),
      system: await this.buildSystemPrompt(tenantId, params.mode),
      messages: await this.buildMessageHistory(params.conversationId),
      tools: { ...ragTools, ...mcpTools },
      maxSteps: maxIterations, // حلقة الوكيل
      onStepStart: async (step) => {
        // تسجيل كل خطوة للمراقبة
        await this.logAgentStep(tenantId, step);
      },
      onStepFinish: async ({ toolCalls, toolResults }) => {
        // التحقق من صلاحيات الأدوات ذات الآثار الجانبية
        for (const call of toolCalls) {
          if (SIDE_EFFECT_TOOLS.includes(call.toolName)) {
            await this.requestUserConfirmation(tenantId, call);
          }
        }
      },
    });

    return {
      response: result.text,
      steps: result.steps,
      citations: this.extractCitations(result.steps),
      toolsUsed: this.extractToolsUsed(result.steps),
      tokensUsed: result.usage,
    };
  }

  private buildRAGTools(tenantId: string) {
    return {
      search_knowledge_base: tool({
        description: 'البحث في قاعدة المعرفة الخاصة',
        parameters: z.object({
          query: z.string(),
          topK: z.number().default(5),
        }),
        execute: async ({ query, topK }) => {
          return hybridSearch({ tenantId, query, topK });
        },
      }),
    };
  }
}
```

---

## 7. تجمع عملاء MCP (MCP Client Pool)

تُستخدم `createMcpHandler(factory)` من `@modelcontextprotocol/server` كنقطة دخول HTTP للإصدار v2 التي تخدم مواصفة 2026-07-28 لكل طلب — وتخدم أيضاً بشكل افتراضي حركة مرور عام 2025 من خلال الأسلوب عديم الحالة المُثبَّت.

```typescript
// /lib/mcp/client-pool.ts
import { Client } from '@modelcontextprotocol/client';
import { StreamableHTTPClientTransport } from '@modelcontextprotocol/client/streamable-http';

// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
// كل عميل Stateless — متوافق مع 2026-07-28
// لا sessions — كل طلب مستقل بالكامل
// tenant_id يُمرر دائماً ضمن معاملات الأداة
// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

export class MCPClientPool {

  private static cache = new Map<string, CachedClient>();

  static async getClient(serverId: string, tenantId: string): Promise<Client> {
    const cacheKey = `${serverId}:${tenantId}`;
    
    // فحص الكاش (مع TTL قصير لأن العملاء stateless)
    const cached = this.cache.get(cacheKey);
    if (cached && !this.isExpired(cached)) {
      return cached.client;
    }

    const config = await getServerConfig(serverId, tenantId);
    const tokens = await getMCPTokens(serverId, tenantId);

    const transport = new StreamableHTTPClientTransport(
      new URL(config.endpoint),
      {
        // رمز الوصول مع Resource Binding (RFC 8707)
        authProvider: async () => ({
          type: 'bearer',
          token: await this.getValidToken(serverId, tenantId),
        }),
        // Headers الجديدة في مواصفة 2026-07-28
        headers: {
          'Mcp-Client-Id': `omnirag-${tenantId}`,
        },
      }
    );

    const client = new Client(
      { name: 'omnirag-client', version: '2.0.0' },
      { capabilities: { tools: {}, resources: {}, prompts: {} } }
    );

    await client.connect(transport);

    // كاش لمدة قصيرة (الـ Stateless لا يحتاج حالة)
    this.cache.set(cacheKey, { 
      client, 
      expiresAt: Date.now() + 60_000 // 60 ثانية
    });

    return client;
  }

  // جلب الأدوات المتاحة من خوادم MCP النشطة
  static async getActiveTools(
    tenantId: string, 
    serverIds: string[]
  ): Promise<Record<string, any>> {
    const tools: Record<string, any> = {};
    
    const clients = await Promise.allSettled(
      serverIds.map(id => this.getClient(id, tenantId))
    );

    for (let i = 0; i < clients.length; i++) {
      if (clients[i].status === 'fulfilled') {
        const serverTools = await clients[i].value.listTools();
        
        for (const t of serverTools.tools) {
          // نطاق الأداة: serverId__toolName لتجنب التعارض
          const scopedName = `${serverIds[i]}__${t.name}`;
          tools[scopedName] = this.wrapTool(
            clients[i].value, 
            t, 
            tenantId
          );
        }
      }
    }

    return tools;
  }

  // تغليف أداة MCP مع التحقق من tenant isolation
  private static wrapTool(client: Client, tool: any, tenantId: string) {
    return {
      description: tool.description,
      parameters: tool.inputSchema,
      execute: async (params: any) => {
        // إضافة tenant_id تلقائياً لكل استدعاء
        const result = await client.callTool({
          name: tool.name,
          arguments: { ...params, tenant_id: tenantId },
        });
        
        // تسجيل الاستدعاء في سجل التدقيق
        await MCPAuditLogger.log({
          tenantId,
          toolName: tool.name,
          params: sanitizeParams(params),
          result: summarizeResult(result),
        });
        
        return result;
      },
    };
  }
}
```

---

## 8. مخطط قاعدة البيانات لطبقة MCP (Neon Postgres)

```sql
-- ═══════════════════════════════════════════════════════
-- جداول طبقة MCP في OmniRAG
-- ═══════════════════════════════════════════════════════

-- تكوينات خوادم MCP المُضافة من قِبل المستخدمين
CREATE TABLE mcp_server_configs (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    tenant_id UUID NOT NULL REFERENCES users(tenant_id),
    
    -- هوية الخادم
    server_id TEXT NOT NULL, -- 'notion', 'github', 'custom-api', ...
    display_name TEXT NOT NULL,
    icon_url TEXT,
    server_type TEXT NOT NULL, -- 'data_source' | 'tool' | 'action' | 'mixed'
    
    -- نقطة الاتصال
    endpoint_url TEXT, -- للخوادم البعيدة
    transport TEXT NOT NULL DEFAULT 'streamable_http', -- 'streamable_http' | 'stdio'
    protocol_version TEXT NOT NULL DEFAULT '2026-07-28',
    
    -- المصادقة (مُشفّرة)
    auth_type TEXT NOT NULL, -- 'oauth2' | 'api_key' | 'none' | 'basic'
    oauth_resource_uri TEXT, -- RFC 8707 Resource Indicator
    credentials_encrypted BYTEA, -- مشفرة بـ AES-256
    
    -- الصلاحيات والضوابط
    enabled_tools TEXT[] DEFAULT '{}', -- أدوات مُفعّلة
    disabled_tools TEXT[] DEFAULT '{}', -- أدوات مُعطّلة
    require_confirmation_tools TEXT[] DEFAULT '{}', -- تتطلب موافقة
    max_calls_per_minute INTEGER DEFAULT 60,
    max_calls_per_day INTEGER DEFAULT 10000,
    
    -- الحالة
    is_active BOOLEAN DEFAULT true,
    last_health_check TIMESTAMPTZ,
    health_status TEXT DEFAULT 'unknown', -- 'healthy' | 'degraded' | 'down' | 'unknown'
    
    created_at TIMESTAMPTZ DEFAULT NOW(),
    updated_at TIMESTAMPTZ DEFAULT NOW(),
    
    UNIQUE(tenant_id, server_id)
);

-- رموز OAuth المخزنة بأمان
CREATE TABLE mcp_oauth_tokens (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    tenant_id UUID NOT NULL REFERENCES users(tenant_id),
    server_config_id UUID NOT NULL REFERENCES mcp_server_configs(id) ON DELETE CASCADE,
    
    -- الرموز (مُشفّرة)
    access_token_encrypted BYTEA NOT NULL,
    refresh_token_encrypted BYTEA,
    
    -- البيانات الوصفية
    scopes TEXT[] DEFAULT '{}',
    resource_uri TEXT, -- RFC 8707: ربط الرمز بالمورد
    issuer TEXT, -- RFC 9207: للتحقق من iss
    expires_at TIMESTAMPTZ,
    
    created_at TIMESTAMPTZ DEFAULT NOW(),
    updated_at TIMESTAMPTZ DEFAULT NOW(),
    
    UNIQUE(tenant_id, server_config_id)
);

-- سجل استدعاءات أدوات MCP (للتدقيق والتحليل)
CREATE TABLE mcp_tool_calls (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    tenant_id UUID NOT NULL REFERENCES users(tenant_id),
    conversation_id UUID REFERENCES conversations(id),
    server_config_id UUID REFERENCES mcp_server_configs(id),
    
    -- تفاصيل الاستدعاء
    tool_name TEXT NOT NULL,
    scoped_tool_name TEXT NOT NULL, -- serverId__toolName
    input_params JSONB,
    output_result JSONB,
    
    -- الأداء
    latency_ms INTEGER,
    status TEXT NOT NULL DEFAULT 'success', -- 'success' | 'error' | 'timeout' | 'cancelled'
    error_message TEXT,
    
    -- الآثار الجانبية
    has_side_effect BOOLEAN DEFAULT false,
    user_confirmed BOOLEAN, -- null إذا لم تكن هناك آثار جانبية
    
    created_at TIMESTAMPTZ DEFAULT NOW()
);

-- نتائج فحص صحة خوادم MCP
CREATE TABLE mcp_health_checks (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    server_config_id UUID NOT NULL REFERENCES mcp_server_configs(id) ON DELETE CASCADE,
    tenant_id UUID NOT NULL REFERENCES users(tenant_id),
    
    status TEXT NOT NULL, -- 'healthy' | 'degraded' | 'down'
    latency_ms INTEGER,
    available_tools_count INTEGER,
    error_message TEXT,
    
    checked_at TIMESTAMPTZ DEFAULT NOW()
);

-- Webhooks الصادرة المُعتمدة
CREATE TABLE mcp_approved_webhooks (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    tenant_id UUID NOT NULL REFERENCES users(tenant_id),
    
    name TEXT NOT NULL,
    endpoint_url TEXT NOT NULL,
    http_method TEXT DEFAULT 'POST',
    headers JSONB DEFAULT '{}',
    secret_hash TEXT, -- للتحقق من HMAC
    
    is_active BOOLEAN DEFAULT true,
    created_at TIMESTAMPTZ DEFAULT NOW()
);

-- تطبيق RLS على جميع جداول MCP
ALTER TABLE mcp_server_configs ENABLE ROW LEVEL SECURITY;
ALTER TABLE mcp_oauth_tokens ENABLE ROW LEVEL SECURITY;
ALTER TABLE mcp_tool_calls ENABLE ROW LEVEL SECURITY;
ALTER TABLE mcp_health_checks ENABLE ROW LEVEL SECURITY;
ALTER TABLE mcp_approved_webhooks ENABLE ROW LEVEL SECURITY;

-- سياسات RLS
CREATE POLICY mcp_tenant_isolation ON mcp_server_configs
    USING (tenant_id = current_setting('app.current_tenant')::UUID);
-- (تُكرر لجميع الجداول)

-- فهارس الأداء
CREATE INDEX idx_mcp_calls_tenant_date 
    ON mcp_tool_calls(tenant_id, created_at DESC);
CREATE INDEX idx_mcp_calls_conversation 
    ON mcp_tool_calls(conversation_id) WHERE conversation_id IS NOT NULL;
CREATE INDEX idx_mcp_server_active 
    ON mcp_server_configs(tenant_id) WHERE is_active = true;
```

---

## 9. نظام التأكيد والموافقة على الإجراءات ذات الآثار الجانبية

وفق أفضل ممارسات MCP، يجب على الأدوات الإفصاح صراحةً عن الآثار الجانبية وتوفير مخططات مدخل/مخرج صريحة.

```
┌──────────────────────────────────────────────────────────────────┐
│              ⚠️ تأكيد الإجراء — قبل التنفيذ                      │
│                                                                  │
│  الوكيل يريد تنفيذ الإجراء التالي:                              │
│                                                                  │
│  🔌 خادم: Slack MCP                                              │
│  🛠️ الأداة: slack_send_message                                    │
│                                                                  │
│  📋 التفاصيل:                                                     │
│  ┌────────────────────────────────────────────────────────────┐  │
│  │  القناة: #team-updates                                      │  │
│  │  الرسالة: "تم تحديث تقرير المبيعات لشهر يوليو. يمكنكم     │  │
│  │           الاطلاع على الملخص في Notion..."                  │  │
│  └────────────────────────────────────────────────────────────┘  │
│                                                                  │
│  ⚡ هذا الإجراء لا يمكن التراجع عنه                               │
│                                                                  │
│  ┌────────────────┐  ┌─────────────────┐  ┌─────────────────┐  │
│  │  ✅ تأكيد      │  │  ✏️ تعديل       │  │  ❌ إلغاء       │  │
│  └────────────────┘  └─────────────────┘  └─────────────────┘  │
│                                                                  │
│  ☐ لا تسألني مجدداً لهذا الخادم في هذه الجلسة                   │
└──────────────────────────────────────────────────────────────────┘
```

---

## 10. إعدادات MCP في صفحة الإعدادات (تحديث `/settings`)

إضافة تبويب **"تكاملات MCP"** إلى صفحة الإعدادات الموجودة:

| الإعداد | الوصف |
|---|---|
| `mcpEnabled` | تفعيل/تعطيل طبقة MCP بالكامل |
| `defaultMCPServers` | الخوادم المُفعّلة افتراضياً في كل محادثة جديدة |
| `agentMaxIterations` | الحد الأقصى لتكرارات الوكيل (1 — 10) |
| `sideEffectPolicy` | سياسة الآثار الجانبية: تأكيد دائماً / تأكيد مرة / رفض تلقائي |
| `mcpCallTimeout` | مهلة انتهاء استدعاء الأداة (5s — 120s) |
| `mcpRateLimit` | إجمالي الاستدعاءات المسموح بها يومياً |
| `auditLogRetention` | مدة الاحتفاظ بسجل استدعاءات MCP |
| `allowedDomains` | قائمة بيضاء لنطاقات خوادم MCP المسموح بها |
| `toolDiscoveryEnabled` | تفعيل اكتشاف أدوات MCP تلقائياً من الجلسة |

---

## 11. تحديث بنية النظام الشاملة (System Architecture — بعد إضافة MCP)

```
┌────────────────────────────────────────────────────────────────────────┐
│                     🌐 OmniRAG — Full System Architecture               │
│                                                                        │
│  ┌──────────────────────────────────────────────────────────────────┐  │
│  │              طبقة العرض (Next.js 16 + App Router)                │  │
│  │  Dashboard | Chat | Sources | MCP-Hub | Knowledge | Analytics   │  │
│  └──────────────────────────┬───────────────────────────────────────┘  │
│                              │                                         │
│  ┌──────────────────────────▼───────────────────────────────────────┐  │
│  │               طبقة API (Next.js API Routes + Edge)               │  │
│  │  /api/chat  /api/sources  /api/mcp  /api/search  /api/settings  │  │
│  └──────┬──────────────┬──────────────┬──────────────┬─────────────┘  │
│         │              │              │              │                │
│         ▼              ▼              ▼              ▼                │
│  ┌─────────────┐ ┌──────────┐ ┌──────────────┐ ┌──────────────┐     │
│  │ RAG Engine  │ │Ingestion │ │  MCP Gateway │ │  Auth + RLS  │     │
│  │ (Hybrid)    │ │Pipeline  │ │  (Stateless  │ │  (Tenant     │     │
│  │             │ │          │ │   2026-07-28)│ │   Isolation) │     │
│  └──────┬──────┘ └────┬─────┘ └──────┬───────┘ └──────────────┘     │
│         │             │              │                                │
│         │             │    ┌─────────┴──────────────────────┐        │
│         │             │    │     MCP Client Pool             │        │
│         │             │    │  (Per-Request, Stateless)       │        │
│         │             │    │                                 │        │
│         │             │    │  ┌────────┐  ┌────────┐        │        │
│         │             │    │  │Notion  │  │GitHub  │  ...   │        │
│         │             │    │  │Client  │  │Client  │        │        │
│         │             │    │  └────────┘  └────────┘        │        │
│         │             │    └─────────────────────────────────┘        │
│         │             │                                               │
│  ┌──────▼─────────────▼────────────────────────────────────────────┐  │
│  │                    طبقة البيانات                                  │  │
│  │  ┌─────────────┐  ┌─────────────┐  ┌────────────────────────┐  │  │
│  │  │   Qdrant    │  │    Neon     │  │    Vercel Blob/S3       │  │  │
│  │  │  (Vectors)  │  │ Postgres    │  │    (Raw Files)          │  │  │
│  │  │             │  │ (+ MCP      │  │                        │  │  │
│  │  │             │  │  tables)    │  │                        │  │  │
│  │  └─────────────┘  └─────────────┘  └────────────────────────┘  │  │
│  └─────────────────────────────────────────────────────────────────┘  │
│                                                                        │
│  ┌─────────────────────────────────────────────────────────────────┐  │
│  │                 الخدمات الخارجية                                  │  │
│  │                                                                  │  │
│  │  ┌──────────────┐  ┌────────────────┐  ┌────────────────────┐  │  │
│  │  │ Google APIs  │  │  Mistral AI    │  │  Unstructured API  │  │  │
│  │  │ (Gemini +    │  │  (Document AI) │  │  (Transform)       │  │  │
│  │  │  Embedding)  │  │                │  │                    │  │  │
│  │  └──────────────┘  └────────────────┘  └────────────────────┘  │  │
│  │                                                                  │  │
│  │  ┌──────────────┐  ┌────────────────┐  ┌────────────────────┐  │  │
│  │  │ MCP Servers  │  │  Zapier /n8n   │  │  OAuth Providers   │  │  │
│  │  │ (22,000+)    │  │  (Automation)  │  │  (Google/GitHub..) │  │  │
│  │  └──────────────┘  └────────────────┘  └────────────────────┘  │  │
│  └─────────────────────────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────────────────────────┘
```

---

## 12. أفضل الممارسات المُطبّقة في طبقة MCP

وفق أفضل ممارسات MCP في الإنتاج: **مسؤولية واحدة** — حد واضح لنطاق كل خادم وحد مصادقة واحد. **مجموعات أدوات محدودة** — أدوات مُركّزة بعقود محددة، يجب تجنب الخوادم الـ "kitchen-sink". **العقود أولاً** — مخططات مدخل/مخرج صارمة، آثار جانبية صريحة، أخطاء موثقة.

وفيما يخص النقل: **stdio للعمليات المحلية** حيث يُولّد العميل الخادم كعملية فرعية، و**Streamable HTTP للخدمات البعيدة والتوسع الأفقي**.

في 2026، يُقلّل اعتماد MCP زمن الاستجابة بنسبة تصل إلى 15% ويزيد الإنتاجية، وتُفضّل المؤسسات MCP للنشر الآمن والقابل للتوسع في RAG والأتمتة والتحليلات.

| الممارسة | التطبيق في OmniRAG |
|---|---|
| **Stateless بالكامل** | كل طلب MCP مستقل — لا sessions — متوافق مع 2026-07-28 |
| **RFC 8707 Resource Indicators** | كل رمز OAuth مُقيّد بـ Resource URI محدد لمنع Token Misuse |
| **RFC 9207 ISS Validation** | التحقق من معامل `iss` في كل تدفق OAuth |
| **Tenant Isolation في كل أداة** | `tenant_id` إلزامي في كل معامل أداة |
| **Audit Log شامل** | تسجيل كل استدعاء أداة مع المدخلات والمخرجات والزمن |
| **Rate Limiting لكل خادم** | حد الاستدعاءات لكل خادم لكل مستخدم |
| **تأكيد الآثار الجانبية** | أي أداة ذات Side Effect تتطلب موافقة صريحة |
| **تشفير الأسرار** | جميع المفاتيح والرموز مُشفّرة بـ AES-256 في قاعدة البيانات |
| **Health Monitoring** | فحص دوري لصحة كل خادم مع تنبيهات الفشل |
| **Scoped Tool Names** | نطاق `serverId__toolName` لتجنب تعارض الأسماء |

---

> **ملاحظة:** طبقة MCP في OmniRAG مصمّمة وفق أحدث مواصفة 2026-07-28 التي جعلت البروتوكول عديم الحالة بالكامل، مما يضمن التوافق مع موازنات التحميل العادية على Vercel دون الحاجة لأي بنية تحتية خاصة. يمكن توسيع هذه الطبقة لتدعم أي من الـ 22,000+ خادم MCP المتاح في السجل الرسمي بإضافة بضعة أسطر إضافة إلى سجل الأدوات.

---
ولّدها مِرْوَر وفق إطار «The New SDLC with Vibe Coding».
كل وثيقة محفوظة داخل مجلدها مع ملف INDEX.md وروابط نسبية إلى ملفات الأقسام المرتبة.
