# تقرير فحص تطبيق OmniRAG والخطة الاحترافية للتطوير

> **التاريخ:** 2026-08-11
> **نطاق الفحص:** منصة OmniRAG — منصة RAG مؤسسية متعددة المستأجرين (Enterprise Agentic RAG Platform)
> **المسار:** `C:\Users\AbuEmad\Downloads\OmniRAG`
> **منهجية الفحص:** خمسة فحوصات متوازية شملت: (1) تحليل البنية والمعمارية، (2) تقييم جودة الكود، (3) التدقيق الأمني الشامل، (4) تقييم واجهة وتجربة المستخدم، (5) تحليل الأداء والدقة.

---

# القسم الأول: تقرير فحص التطبيق

## 1. الملخص التنفيذي

تطبيق **OmniRAG** هو منصة RAG (توليد معزز بالاسترجاع) مؤسسية متعددة المستأجرين، مبنية بأحدث التقنيات (Next.js 16، React 19، TypeScript، Gemini AI، Qdrant، PostgreSQL، Firebase)، وتتميز بمفاهيم متقدمة نادراً ما تُرى في مشاريع بهذه المرحلة: بحث هجين (دلالي + معجمي)، تقنية HyDE، محرك حماية حتمي من 12 نقطة تفتيش (HookHarness)، تكامل مع أدوات MCP، وعزل مستأجرين متعدد الطبقات، ودعم عربي أصيل يشمل التدوين الرياضي العربي، إضافة إلى توثيق SDLC استثنائي يغطي 8 مراحل كاملة.

**ومع ذلك**، فإن التطبيق في حالته الراهنة أقرب إلى **نموذج أولي متقدم (Advanced Prototype)** منه إلى نظام إنتاجي، وذلك بسبب:

- **ثغرات أمنية حرجة** تجعله غير صالح للنشر: قاعدة بيانات Firestore مفتوحة بالكامل للجميع، غياب تام للمصادقة على جميع واجهات API (ثغرة IDOR/BOLA)، ومفتاح Google API مسرب في الكود والسجلات.
- **غياب كامل للاختبارات** رغم وجود وثائق اختبارات، ما يجعل أي تعديل مخاطرة غير محسوبة.
- **مشاكل دقة جوهرية** في منطق الاسترجاع: خوارزمية RRF غير مطبقة فعلياً، عشوائية في ترتيب النتائج، متجهات مشوهة، ومقاييس تحليلات وهمية.
- **مكونات عملاقة وتكرار واسع** يهددان قابلية الصيانة (ملفات تتجاوز 1600 سطر، وبيانات أولية مكررة حرفياً في ملفين).

### جدول التقييم العام

| محور الفحص | الدرجة (من 10) | الحالة | أبرز ملاحظة |
|---|---|---|---|
| البنية والمعمارية | 6.5 | 🟡 جيد مفاهيمياً | معمارية طبقية نظيفة، لكن بازدواجية توجيه غير مبررة (Next.js + Vite) |
| جودة الكود وقابلية الصيانة | 4.5 | 🔴 يحتاج تدخلاً | تكرار صارخ، ملفات عملاقة، 78 استخداماً لـ `any`، لا اختبارات إطلاقاً |
| الأمان | 2.0 | 🔴 حرج | قاعدة بيانات مفتوحة للعموم، لا مصادقة على API، مفاتيح مسربة |
| واجهة وتجربة المستخدم (UI/UX) | 6.0 | 🟡 واعد | تصميم عصري جذاب ودعم عربي فريد، لكن وصولية معدومة وRTL مكسور جزئياً |
| الأداء | 4.0 | 🔴 ضعيف | استعلامات N+1، كتابات تسلسلية ثلاثية، غياب التخزين المؤقت |
| الدقة (صحة النتائج) | 4.0 | 🔴 ضعيف | RRF غير حقيقي، نتائج عشوائية، متجهات مشوهة، أدوات MCP محاكاة |
| **المتوسط العام** | **4.5** | **🔴 نموذج أولي متقدم** | **يحتاج معالجات جوهرية قبل أي نشر إنتاجي** |

### توزيع الثغرات الأمنية حسب الخطورة

| مستوى الخطورة | العدد | أمثلة |
|---|---|---|
| 🔴 حرجة (Critical) | 3 | قواعد Firestore المفتوحة، غياب المصادقة عن API، مفتاح API مسرب |
| 🟠 عالية (High) | 4 | أسرار مثبتة في الواجهة، تعطيل TLS، غياب Rate Limiting، رفع ملفات غير آمن |
| 🟡 متوسطة (Medium) | 6 | كوكيز بلا أعلام حماية، تسريب رسائل الأخطاء، SSRF محدود، معرفات بـ Math.random |
| 🟢 منخفضة (Low) | 3 | مفاتيح تجريبية تحاكي أنماطاً حقيقية، إفصاح إصدارات، نطاقات تطوير متساهلة |
| **الإجمالي** | **16** | الاعتماديات: 0 ثغرات CVE معروفة (npm audit) |

---

## 2. الفحص الأول: تحليل البنية والمعمارية

### 2.1 شجرة هيكل المشروع (مختصرة)

```
OmniRAG/
├── .env.example                  # نموذج متغيرات البيئة
├── dev-server.js / server.ts     # خوادم تطوير وإنتاج
├── firebase-applet-config.json   # إعدادات Firebase (⚠️ مفتاح مكشوف)
├── firestore.rules               # ⚠️ قواعد مفتوحة بالكامل
├── index.html / vite.config.ts   # نقطة دخول Vite (موازية)
├── next.config.ts                # إعدادات Next.js (⚠️ ignoreBuildErrors)
├── package.json / bun.lock
├── tsconfig.json                 # TypeScript strict + paths @/*
├── SDLC/                         # توثيق دورة حياة تطوير كامل (8 مراحل)
└── src/                          # ~17,203 سطر في 75 ملف TS/TSX
    ├── main.tsx / App.tsx        # نقطة دخول Vite/React Router
    ├── app/                      # Next.js App Router + 16 مسار API في /api/v1/
    ├── components/               # 20+ مكوّن (أكبرها ChatStudio بـ 1,497 سطر)
    ├── lib/
    │   ├── firebase.ts + auth/   # مصادقة Firebase
    │   ├── harness/              # محرك HookHarness الأمني (12 خطافاً)
    │   ├── rag/                  # محرك RAG (HyDE + بحث هجين + أدوات MCP)
    │   ├── storage/              # Firestore + PostgreSQL + Qdrant + Memory
    │   └── types/                # تعريفات أنواع شاملة (22 واجهة)
    └── views/                    # صفحات react-router القديمة
```

### 2.2 التقنيات والاعتماديات الرئيسية

| الفئة | التقنية | الإصدار |
|---|---|---|
| الإطار | Next.js (App Router) | ^16.3.0 |
| المكتبة | React | ^19.0.0 |
| اللغة | TypeScript (strict) | ^5.7.0 |
| بناء بديل | Vite + tsx | ^6.0.0 / ^4.19.0 |
| التنسيق | Tailwind CSS | ^4.0.0 |
| الذكاء الاصطناعي | @google/genai + Vercel AI SDK | ⚠️ `*` / ^4.0.39 |
| قواعد البيانات | Firebase Firestore، PostgreSQL (pg)، Qdrant | ^10.14.1 / ^8.13.0 / ^1.11.0 |
| الوسائط | Remotion، KaTeX، react-markdown | ^4.0.507 / ^0.18.4 / ^10.1.0 |
| الحركات والأيقونات | motion، lucide-react | ^11.15.0 / ^0.468.0 |

### 2.3 الوصف المعماري

معمارية رباعية الطبقات:

1. **طبقة العرض:** `MainApp` يدير 9 تبويبات (هبوط، محادثة، نماذج، معرفة، MCP، بحث، أمن، تحليلات، إعدادات) مع مصادقة Firebase.
2. **طبقة API:** 16 مساراً في `/api/v1/`، أهمها `chat/completions` الذي ينفذ: `HookHarness(pre_auth → pre_inference)` ← بحث هجين ← توليد Gemini ← `HookHarness(post_inference)`.
3. **طبقة منطق الأعمال:** محرك HookHarness (12 خطافاً: عزل مستأجر، كشف حقن Prompt بالعربية/الإنجليزية، بوابة موافقة بشرية للأدوات ذات الأثر الجانبي، إخفاء PII، تدقيق)، ومحرك RAG (توجيه ذكي للنموذج، HyDE، دمج هجين Qdrant+Postgres FTS)، وسجل نماذج قابل للتخصيص.
4. **طبقة البيانات:** طبقة تجريد `OmniRAGDatabase` فوق Firestore مع بديل ذاكرة تلقائي وبذر بيانات أولية، وPostgreSQL مع RLS وبحث معجمي، وQdrant للبحث الدلالي (3072 بُعداً).

### 2.4 ملاحظات التنظيم المعماري

**نقاط القوة:**
- فصل طبقي نظيف (عرض/API/منطق/بيانات) وتغطية TypeScript صارمة مع أنواع مركزية شاملة.
- نظام أمني استباقي متميز التصميم (HookHarness) بسجلات تدقيق لكل حدث.
- عزل مستأجرين متعدد المستويات (Firestore + PostgreSQL RLS + Qdrant filters).
- مرونة تشغيل عالية: تراجع تلقائي ثلاثي المستويات عند غياب الخدمات.
- توثيق SDLC احترافي نادر (8 مراحل، 20+ وثيقة).
- دعم عربي أصيل (RTL، MathJax4Arabic، كشف حقن بالعربية).

**نقاط الضعف:**
- ازدواجية معمارية: نظاما توجيه متعارضان (Next.js App Router + Vite/React Router) بنقاط دخول متعددة (`main.tsx`, `page.tsx`, `server.ts`, `dev-server.js`).
- عدم اتساق الإعدادات: alias `@` يشير لجذر المشروع في Vite ولـ `./src` في tsconfig.
- `typescript.ignoreBuildErrors: true` يلغي فحص الأنواع عند البناء.
- اعتمادية `@google/genai: "*"` (أي إصدار) — خطر على استقرار البناء.
- عدم تطابق أرقام الإصدارات (package.json: v0.1.9 مقابل README: v0.1.8).
- سكربتات npm بصيغة يونكس لا تعمل على Windows (`rm -rf`).

---

## 3. الفحص الثاني: تقييم جودة الكود

**الدرجة: 4.5 / 10**

### 3.1 نقاط القوة

- نمط HookHarness الأمني ممتاز التصميم: مراحل واضحة، union types منضبطة، وفصل الأنماط في ثوابت — أفضل جزء معماري في المشروع.
- طبقة أنواع مركزية قوية (`omnirag.ts`): 22 واجهة ونوعاً تغطي كامل النطاق بأنواع حرفية دقيقة.
- فصل جيد للاهتمامات في التخزين: وحدات مستقلة لـ Postgres وQdrant والتضمين، مع استعلامات SQL مُعامَلَة بأمان وتفعيل RLS.
- معالجة تراجع متدرجة (graceful degradation) في التضمينات مع تطبيع الأبعاد.
- تسمية معبّرة وتوثيق JSDoc في المحركات الأساسية.

### 3.2 نقاط الضعف والروائح البرمجية (Code Smells)

| # | المشكلة | الخطورة | المواضع |
|---|---|---|---|
| 1 | تكرار صارخ: البيانات الأولية منسوخة حرفياً (~20KB) بين ملفين | عالية | `db.ts` (1628 سطر) ↔ `constants.ts` (481 سطر) |
| 2 | نمط "التراجع للذاكرة" مكرر حرفياً أكثر من 25 مرة | عالية | جميع دوال `OmniRAGDatabase` في `db.ts` |
| 3 | ملفات عملاقة تمزج المسؤوليات (God Objects) | عالية | `db.ts` (1628)، `ChatStudio.tsx` (1497)، `SourcesDashboard.tsx` (1477)، `McpGateway.tsx` (1141)، `DocumentIngestionStudio.tsx` (1127) |
| 4 | ابتلاع الأخطاء: 155 كتلة catch تسجل بـ console فقط؛ تحويل صامت للذاكرة المؤقتة = خطر فقدان بيانات بلا علم المستخدم | عالية | `db.ts`، `chat/stream/route.ts` (catch فارغ)، `embedding.ts` |
| 5 | 78 استخداماً للنوع `any` (23 في lib) | عالية | `db.ts`، `engine.ts`، `postgres.ts`، `qdrant.ts` |
| 6 | غياب كامل للاختبارات: لا ملفات test/spec، لا jest/vitest، لا سكربت test | حرجة | المشروع كاملاً |
| 7 | منطق محاكاة وبيانات عميل خاص مدمجة في المحرك العام (مستودع MisterAI-Yemen حالة خاصة مبرمجة!) | عالية | `engine.ts` دالة `executeMcpTool` (~السطور 95-300) |
| 8 | 16 استخداماً لـ `Math.random` في منطق الخادم (درجات بحث، مدد مزامنة) | عالية | `engine.ts`، `db.ts` |
| 9 | لا ESLint ولا Prettier إطلاقاً؛ سكربت lint مجرد `tsc --noEmit` | متوسطة | `package.json` |
| 10 | تسمية مضللة: `performHybridSearch` يدّعي RRF وهو دمج خطي موزون | متوسطة | `engine.ts` |
| 11 | أرقام سحرية وثوابت مدمجة (tenantId الافتراضي مكرر في 19 مساراً) | متوسطة | مسارات API، `engine.ts`، `db.ts` |
| 12 | ترميز مشوه (mojibake) لنصوص عربية مدمجة | عالية | `ChatStudio.tsx`، `db.ts`، `hook-harness.ts` |

---

## 4. الفحص الثالث: التدقيق الأمني الشامل

**مستوى الخطورة الإجمالي: حرج (CRITICAL) — التطبيق غير صالح للنشر الإنتاجي بحالته الحالية.**

### 4.1 الثغرات الحرجة

| # | الثغرة | الملف والسطر | التوصية الإصلاحية |
|---|---|---|---|
| C1 | قواعد Firestore مفتوحة للجميع: `allow read, write: if true` — أي شخص يقرأ/يعدّل/يحذف كامل قاعدة البيانات | `firestore.rules:4-5` | ربط القواعد بـ `request.auth` مع عزل المستأجرين: `allow read, write: if request.auth != null && tenantId == 'tenant-' + request.auth.uid` |
| C2 | غياب كامل للمصادقة عن جميع مسارات API + ثغرة IDOR/BOLA: `tenantId` يُؤخذ من جسم الطلب/الترويسة مع قيمة افتراضية، وأي مهاجم يصل لبيانات أي مستأجر ويستهلك رصيد Gemini المدفوع | جميع مسارات `src/app/api/v1/*` (مثل `chat/completions/route.ts:10`، `sources/route.ts:11`) | middleware مركزي يتحقق من Firebase ID Token عبر Admin SDK ويشتق `tenantId` من `decodedToken.uid` حصرياً، ورفض غير المصادق بـ 401 |
| C3 | مفتاح Google API حقيقي مثبت في الكود ومسرب في سجلات الطرفية | `firebase-applet-config.json:4` + `terminal_logs/blocking_commands.log` (أسطر متعددة) | إبطال المفتاح فوراً وتدويره، تقييده بـ API restrictions، نقل التكوين لمتغيرات بيئة، تعقيم السجلات وتنظيف تاريخ Git |

### 4.2 الثغرات العالية

| # | الثغرة | الملف والسطر | التوصية الإصلاحية |
|---|---|---|---|
| H1 | أسرار مثبتة في كود الواجهة: مفتاح تجريبي وكلمة مرور تجريبية `Password123!` | `SdlcPage.tsx:68`، `AuthScreen.tsx:38` | حذف القيم المثبتة، استخدام متغيرات بيئة، تعطيل الحساب التجريبي في الإنتاج |
| H2 | تعطيل التحقق من شهادات TLS في PostgreSQL (ثغرة MITM) | `postgres.ts:17` — `rejectUnauthorized: false` | تعيين `true` مع تمرير شهادة CA الموثوقة |
| H3 | غياب تام لتحديد المعدل (Rate Limiting): استنزاف اقتصادي لفاتورة API، تخمين كلمات مرور، DoS | جميع مسارات API وشاشة الدخول | Upstash Ratelimit أو Redis middleware بحدود لكل مسار (10 طلبات/دقيقة للدردشة، 5 محاولات دخول/15 دقيقة) |
| H4 | معالجة غير آمنة لرفع الملفات: قبول base64 حتى 50MB دون تحقق من النوع الفعلي | `documents/parse/route.ts:26-40`، `next.config.ts` | حد أقصى 10MB، قائمة بيضاء MIME مع فحص magic bytes |

### 4.3 الثغرات المتوسطة والمنخفضة

| # | الثغرة | الموضع | التوصية |
|---|---|---|---|
| M1 | كوكي إعدادات بدون `httpOnly`/`secure` | `settings/models/route.ts:49-53` | إضافة الأعلام |
| M2 | إفصاح رسائل الأخطاء الداخلية `err.message` للعملاء | معظم المسارات | رسالة عامة للعميل + تفاصيل في السجلات الداخلية |
| M3 | SSRF محدود في جلب ترجمة يوتيوب (baseUrl يُجلب دون تحقق) | `youtube/transcript/route.ts:100-118` | التحقق من النطاق + مهلة زمنية |
| M4 | `.gitignore` ناقص أنماط حساسة | `.gitignore` | إضافة `*.pem`, `*.key`, `terminal_logs/` |
| M5 | معرفات بـ `Math.random` (قابلة للتنبؤ والتصادم) | `hook-harness.ts:166`، `engine.ts` عدة مواضع | `crypto.randomUUID()` |
| M6 | اعتماديات الموصلات تُخزن بنص صريح في Firestore | `sources/route.ts:46-62` | تشفير AES-256-GCM أو مدير أسرار |
| L1 | قيم تجريبية تحاكي أنماط مفاتيح حقيقية | `sources/capabilities/route.ts:43` | قيم placeholder محايدة |
| L2 | مسار الصحة يكشف إصدارات الإطار | `api/health/route.ts:8-10` | الاكتفاء بـ `{status:'ok'}` |
| L3 | `allowedDevOrigins` بنطاق شامل `*.run.app` | `next.config.ts:9-14` | تقييد النطاقات |

### 4.4 نقاط القوة الأمنية (تستحق الإشادة والتوسيع)

- استعلامات PostgreSQL كلها مُعامَلَة (parameterized) مع RLS — لا حقن SQL مباشر.
- دفاعات Prompt Injection بأنماط عربية/إنجليزية، وبوابة موافقة بشرية للأدوات ذات الأثر الجانبي.
- تعقيم PII بعد الاستدلال، وسجلات تدقيق للأحداث الأمنية.
- `npm audit`: 0 ثغرات CVE معروفة في الاعتماديات.

> ⚠️ **ملاحظة جوهرية:** هذه الدفاعات تفقد قيمتها عملياً بسبب غياب المصادقة الحقيقية (C2)؛ فالهوية تُؤخذ من العميل، وخطاف `pre_auth` يتحقق من وجود `tenantId` كنص فقط لا من هوية المستخدم.

---

## 5. الفحص الرابع: تقييم واجهة وتجربة المستخدم (UI/UX)

**الدرجة: 6 / 10**

### 5.1 الواجهة الحالية

واجهة ويب كاملة تتكون من: صفحة هبوط داكنة عصرية بفيديو Remotion، شاشة مصادقة ثنائية اللغة (Google + MFA تجريبي)، غلاف رئيسي بتسع وحدات وظيفية عبر تبويبات، وعارض رسائل غني يدعم Markdown وKaTeX والتدوين الرياضي العربي والأرقام العربية المشرقية والقراءة الناطقة TTS.

### 5.2 نقاط القوة

- تصميم بصري عصري جذاب: تدرجات، زجاجية (backdrop-blur)، حركات دقيقة، وأنيميشن Remotion.
- دعم عربي/إنجليزي شامل مع مبدّل لغة وتعريب حقيقي، وأسهم تراعي الاتجاه.
- **ميزة فريدة عالمياً:** التدوين الرياضي العربي وتحويل الأرقام والرموز الرياضية للعربية مع KaTeX — تميّز نادر في منصات RAG.
- تغذية راجعة ممتازة: حالات تحميل ونجاح/خطأ وحالات فارغة مصممة ومؤشر نبض للخادم.
- تجربة محادثة متقدمة: استشهادات، موافقات أدوات MCP، لوحة سجلات جانبية.
- تجاوب جيد نسبياً عبر breakpoints في 12+ مكوّناً.

### 5.3 نقاط الضعف والفجوات

| المجال | المشكلة | التقييم الفرعي |
|---|---|---|
| إمكانية الوصول (A11y) | **صفر `aria-label`** في الكود كله، لا أدوار ARIA، لا تنقل بلوحة المفاتيح، أزرار أيقونية تعتمد tooltips بالتحويم فقط، لا رابط تخطٍّ — مخالفة جوهرية لـ WCAG 2.2 | 1/10 |
| دعم RTL | كلاسات `dir-rtl`/`dir-ltr` غير معرّفة في CSS إطلاقاً، لا خصائص منطقية (`ms-/me-`) في Tailwind، خصائص فيزيائية (`ml-2`) تكسر التخطيط، `index.html` ما زال `lang="en"` بعنوان افتراضي | 5/10 |
| الهوية البصرية | غلافان وموجّهان متوازيان (Navbar داكن "Next.js v16" مقابل Header فاتح "OmniRAG")، 3 شعارات مختلفة (Ω / N / Layers)، إصدارات متضاربة (v0.1.1 / v0.1.8 / v2.4)، لا مبدّل سمة داكن/فاتح | 4/10 |
| نظام التصميم | لا Design Tokens، لا ملف tailwind.config مخصص، مجلد `ui/` فيه مكوّن واحد فقط | 3/10 |
| الخطوط | لا خط عربي احترافي (Cairo/Tajawal/IBM Plex Arabic)، وكلاس `arabic-math-formula` يشير لخط Amiri غير مستورد فعلياً | 4/10 |
| التجاوب | جيد عموماً، لكن شريط تنقل الجوال بلا مؤشرات تمرير ولا قائمة همبرغر | 7/10 |

---

## 6. الفحص الخامس: تحليل الأداء والدقة

**الأداء: 4 / 10 | الدقة: 4 / 10**

### 6.1 أبرز مشاكل الأداء

| # | المشكلة | الموضع | التأثير | التحسين المقترح |
|---|---|---|---|---|
| P1 | استعلام N+1 عند جلب عناوين المستندات (استدعاء متسلسل لكل نتيجة) | `engine.ts:411` | زمن استجابة يتضاعف خطياً مع النتائج | جلب دفعي واحد + بناء Map |
| P2 | تحميل كامل مقاطع المستأجر للذاكرة في المسار الاحتياطي بفلترة O(N×M) | `engine.ts:443` | لا يتوسع إطلاقاً مع نمو البيانات | Postgres FTS إلزامي + MiniSearch/FlexSearch محلياً |
| P3 | كتابات تسلسلية رباعية لكل مقطع (Firestore ← embedding ← Postgres ← Qdrant) — مستند 50 مقطعاً = 200 عملية شبكة متتالية | `documents/route.ts:76-90`، `db.ts:addChunk` | دقائق لاستيعاب مستند + مهلة serverless شبه مؤكدة | Batch embedding، upsert دفعي، Firestore batch، أو طابور خلفية |
| P4 | إعادة إنشاء عميل GoogleGenAI في كل تضمين + غياب أي كاش | `embedding.ts:19` | حمل وزمن وتكلفة مضاعفة | Singleton + كاش LRU بمفتاح hash(model+text) |
| P5 | فحص/تهيئة مخازن البيانات في كل عملية (رحلة شبكة إضافية لكل CRUD) | `qdrant.ts`، `postgres.ts`، `db.ts:ensureSeeded` (~30 موضعاً) | زمن إضافي متراكم | وعد تهيئة واحد memoized عند الإقلاع |
| P6 | قاعدة الذاكرة غير محدودة النمو ونسخ خطي O(n) لكل إضافة | `db.ts:MemoryDatabase` | تسرب ذاكرة فعلي + تدهور كتابة | Map بدل مصفوفات + حد أقصى مع إخلاء LRU |
| P7 | مسار التحليلات يجلب كل البيانات لحساب عدّادات | `analytics/route.ts:9-12` | نقل بيانات ضخم | استعلامات COUNT تجميعية |
| P8 | رفع ملفات base64-in-JSON حتى 50MB (50MB+ ذاكرة لكل طلب تحليل — خطر DoS) | `documents/parse/route.ts` | استهلاك ذاكرة الخادم | multipart/stream + حد 10MB + طابور |
| P9 | 136 استدعاء console في مسارات ساخنة | `src` كاملاً | إبطاء I/O متزامن | logger منظم (pino) بمستويات |

### 6.2 أبرز مشاكل الدقة ومنطق المعالجة

| # | المشكلة | الموضع | التأثير | التحسين المقترح |
|---|---|---|---|---|
| A1 | عشوائية `Math.random()` في درجات البحث — نفس الاستعلام يرتّب مختلفاً كل مرة | `engine.ts:478` | دقة غير قابلة للقياس أو الاختبار | درجات حتمية + ترتيب ثابت عند التعادل |
| A2 | خوارزمية "RRF" ليست RRF — دمج خطي موزون لدرجات خام على مقاييس مختلفة | `engine.ts:419,481` | هيمنة المسار الأكبر مقياساً وترتيب خاطئ منهجياً | RRF حقيقي: `Σ 1/(60+rank)` أو تطبيع min-max |
| A3 | "الدرجة الدلالية" المحاكاة ليست دلالية (تكرار مصطلحات معجمية مرة أخرى) — لا بحث دلالي حقيقي في وضع المحاكاة | `engine.ts:464-478` | المرادفات بلا تطابق حرفي لن تُسترجع — فشل في وعد RAG | تخزين المتجه مع المقطع في الذاكرة + تشابه كوسيني فعلي |
| A4 | تطبيع 3072 بتكرار المتجه حلقياً يفسد الدلالات، والمتجه الاحتياطي (تجزئة محارف) يُرفع لـ Qdrant كأنه حقيقي | `embedding.ts:normalizeTo3072` | نتائج Qdrant عشوائية عملياً بصمت | مطابقة الأبعاد مع النموذج الفعلي + رفض الرفع عند عدم التطابق |
| A5 | فشل صامت في التضمينات دون إعادة محاولة أو تنبيه | `embedding.ts:17-29` | تدهور دلالي صامت دون مؤشر للمشغّل | إعادة محاولة أسّية + حالة فهرسة لكل مقطع + تنبيه |
| A6 | مقاييس تحليلات وهمية ثابتة (p95=240، MRR=0.92، Recall=0.96) | `analytics/route.ts:19-21` | قرارات مبنية على بيانات باطلة | حساب فعلي من زمن الاستعلامات + مجموعة تقييم معلَّمة |
| A7 | أدوات MCP كلها محاكاة ببيانات مفبركة يقدمها النموذج كحقائق (روابط وأرقام مختلقة) | `engine.ts:executeMcpTool` | خطر قرارات على معلومات ملفقة | موصلات حقيقية + تمييز صريح لوضع المحاكاة |
| A8 | تقدير الرموز بالتقريب الخام (`length/4`) وأرقام استهلاك ثابتة | `engine.ts` | فوترة وحصص غير دقيقة | قراءة `usageMetadata` الفعلية |
| A9 | **ترميز عربي مشوه في أنماط كشف الحقن والبيانات الأولية — خط الدفاع العربي معطّل فعلياً** | `hook-harness.ts`، `db.ts` | أنماط Regex العربية لن تطابق نصاً حقيقياً (مشكلة أمنية-دقيقة) | إعادة كتابة بترميز UTF-8 + اختبار وحدة بعينات هجوم عربية |
| A10 | فهرس FTS إنجليزي فقط + قاموس كمعامل يمنع استخدام الفهرس + قاموس عربي غير مضمون | `postgres.ts` | بحث عربي بطيء (seq scan) أو ضعيف (ILIKE) | فهرسان تعبيريان ثابتان + pg_trgm للعربية |
| A11 | نص HyDE الطويل يُمرر للبحث المعجمي فيُفسد المطابقة | `engine.ts:~341` | HyDE يضر الفرع المعجمي | HyDE للدلالي فقط + الاستعلام الأصلي للمعجمي |
| A12 | تجاوز خطاف `pre_tool` عند تنفيذ الأدوات — أداة معطّلة قد تُنفَّذ | `engine.ts:generateRagCompletion` | ثغرة منطقية في تطبيق السياسات | توجيه كل استدعاء عبر HookHarness |
| A13 | تقطيع المستندات بالأحرف يقطع الكلمات والجمل حتى مع `strategy='semantic'` | `documents/route.ts:60-72` | تدهور التضمين والاسترجاع والاقتباسات | تقطيع على حدود الجمل/الفقرات مع مراعاة الترقيم العربي |
| A14 | تصادم محتمل في معرفات Qdrant (تجزئة 32-bit) | `qdrant.ts` | كتابة فوق نقاط = فقدان بيانات صامت | UUID v5 حتمي |

---

## 7. الخلاصة والحكم العام

المشروع **طموح بوعي معماري عالٍ** ومفاهيم متقدمة حقيقية (RRF، HyDE، MCP، Guardrails، عزل مستأجرين متعدد الطبقات) وتوثيق استثنائي ودعم عربي متميز، لكنه يقف عند مرحلة **النموذج الأولي المتقدم** ولا يجوز نشره إنتاجياً قبل:

1. **إطفاء الحرائق الأمنية الثلاث** (قواعد Firestore، مصادقة API، تدوير المفاتيح).
2. **إصلاح الترميز العربي** في hook-harness وdb.ts (أمني وفوري).
3. **بناء أساس الجودة** (اختبارات للوحدات الحرجة، ESLint/Prettier، CI).
4. **تصحيح دقة الاسترجاع** (RRF حقيقي، إزالة العشوائية، منع المتجهات المشوهة).
5. **توحيد المعمارية** (Next.js فقط) وتقسيم الملفات العملاقة.

---
---

# القسم الثاني: الخطة الاحترافية للتطوير

## 1. رؤية الخطة ومبادئها

**الهدف الاستراتيجي:** تحويل OmniRAG من نموذج أولي متقدم إلى **منصة إنتاجية مؤسسية قوية ومتينة ومرنة** — آمنة افتراضياً (Secure by Default)، دقيقة قابلة للقياس، عالية الأداء قابلة للتوسع، سهلة التطوير والصيانة، وبواجهة إبداعية عصرية متاحة للجميع.

**المبادئ الحاكمة:**
- **الأمان أولاً:** لا ميزة جديدة قبل إغلاق الثغرات الحرجة.
- **القياس قبل التحسين:** لا دقة ولا أداء بلا مقاييس حقيقية واختبارات.
- **التوحيد قبل التوسيع:** معمارية واحدة، مصدر حقيقة واحد لكل بيانات.
- **تحسينات تدريجية قابلة للشحن:** كل مرحلة تنتج قيمة ملموسة دون كسر النظام.

---

## 2. المرحلة الأولى — قصيرة المدى (الأسابيع 1–4): "إطفاء الحرائق وبناء الأساس"

### البند 1.1: إغلاق الثغرات الأمنية الحرجة

- **الهدف:** جعل التطبيق آمناً للنشر التجريبي المغلق بإغلاق الثغرات C1–C3 وH1–H4.
- **الخطوات التنفيذية:**
  1. إبطال مفتاح Google المسرب فوراً من Google Cloud Console وإصدار مفتاح جديد مقيّد (API restrictions + HTTP referrers).
  2. إعادة كتابة `firestore.rules` بعزل مستأجرين مربوط بـ `request.auth`، ونشرها واختبارها بمحاكي قواعد الأمان.
  3. بناء middleware مصادقة مركزي (`withAuth`) يتحقق من Firebase ID Token عبر Admin SDK ويشتق `tenantId` من التوكن فقط، وتطبيقه على كل مسارات `/api/v1`.
  4. حذف الأسرار المثبتة من الواجهة، ونقل التكوين إلى متغيرات بيئة، وتعقيم `terminal_logs/` وإضافته لـ `.gitignore` مع أنماط `*.pem, *.key, *.p12`.
  5. تفعيل `rejectUnauthorized: true` لاتصال PostgreSQL مع شهادة CA.
  6. تطبيق Rate Limiting مركزي بحدود متمايزة لكل مسار.
  7. تقييد رفع الملفات (10MB، قائمة بيضاء MIME + فحص magic bytes).
- **الأدوات والتقنيات:** Firebase Admin SDK، Firebase Emulator Suite (اختبار القواعد)، Upstash Ratelimit، git-filter-repo، Google Secret Manager، cross-env.
- **الأولوية:** 🔴 حرجة (P0) — تسبق كل شيء.
- **الأثر المتوقع:** صفر ثغرات حرجة/عالية؛ الانتقال من "مفتوح للعالم" إلى "آمن افتراضياً"؛ حماية فاتورة API والبيانات.

### البند 1.2: إصلاح الترميز العربي وتنظيف البيانات المسمومة

- **الهدف:** إعادة تفعيل خط الدفاع العربي ضد Prompt Injection وإظهار البيانات الأولية بشكل صحيح.
- **الخطوات التنفيذية:**
  1. إعادة كتابة `hook-harness.ts` و`db.ts` و`ChatStudio.tsx` بترميز UTF-8 سليم (فحص جميع الملفات بأداة كشف ترميز).
  2. إضافة اختبار وحدة يتحقق من تطابق أنماط الحقن العربية مع عينات هجوم حقيقية.
  3. إعادة بذر البيانات الأولية النظيفة وتوحيدها في مصدر واحد (حذف التكرار بين `constants.ts` و`db.ts`).
- **الأدوات والتقنيات:** VS Code encoding، أيقونv/chardet للفحص الآلي، Vitest.
- **الأولوية:** 🔴 حرجة (P0).
- **الأثر المتوقع:** استعادة الحماية العربية الفعلية؛ تجربة عربية سليمة من أول تشغيل.

### البند 1.3: بناء البنية التحتية للجودة

- **الهدف:** منع تراجع الجودة مستقبلاً عبر أدوات فرض آلية.
- **الخطوات التنفيذية:**
  1. إعداد ESLint (قواعد Next.js + TypeScript strict + react-hooks) وPrettier مع تنسيق كامل للمشروع.
  2. تفعيل `ignoreBuildErrors: false` وإصلاح أخطاء الأنواع الناتجة، وإزالة أخطر استخدامات `any` (23 في lib).
  3. إعداد Vitest + Testing Library وكتابة أول 30 اختباراً للوحدات الحرجة: HookHarness (12 خطافاً)، خوارزمية الدمج، التضمين.
  4. إعداد GitHub Actions CI: lint + typecheck + test + npm audit + secrets scanning عند كل push.
  5. تثبيت الإصدارات الدقيقة في package.json (إزالة `*` و`^` الخطرة) وتفعيل Dependabot.
  6. إصلاح سكربتات npm للتوافق مع Windows (cross-env، rimraf).
- **الأدوات والتقنيات:** ESLint، Prettier، Vitest، @testing-library/react، GitHub Actions، Husky + lint-staged، Dependabot، gitleaks.
- **الأولوية:** 🔴 حرجة (P0).
- **الأثر المتوقع:** شبكة أمان تجعل أي تعديل لاحق منخفض المخاطرة؛ رفع درجة الجودة من 4.5 إلى 6.5.

### البند 1.4: إصلاح دقة الاسترجاع الحرجة

- **الهدف:** جعل نتائج البحث حتمية وصحيحة الخوارزمية وقابلة للاختبار.
- **الخطوات التنفيذية:**
  1. إزالة كل `Math.random()` من منطق الخادم واستبدالها بدرجات حتمية و`crypto.randomUUID()` للمعرفات.
  2. تطبيق RRF حقيقي (`Σ 1/(60+rank)`) على قائمتي الترتيب الدلالية والمعجمية.
  3. منع رفع المتجهات الاحتياطية/المشوهة إلى Qdrant؛ مطابقة أبعاد المجموعة مع النموذج الفعلي ورفض عدم التطابق بصراحة.
  4. تخزين المتجه مع المقطع في MemoryDatabase وحساب تشابه كوسيني فعلي في وضع المحاكاة.
  5. فصل نص HyDE: للفرع الدلالي فقط، وإبقاء الاستعلام الأصلي للفرع المعجمي.
  6. إنشاء فهارس FTS تعبيرية ثابتة (english + simple/arabic) ودعم pg_trgm للعربية.
  7. توجيه كل استدعاء أداة عبر خطاف `pre_tool`، وتمييز نتائج المحاكاة بشارة واضحة للمستخدم.
- **الأدوات والتقنيات:** خوارزمية RRF معيارية، `output_dimensionality` في Gemini Embedding، pg_trgm، UUID v5.
- **الأولوية:** 🔴 حرجة (P0) — تلامس جوهر قيمة المنتج.
- **الأثر المتوقع:** رفع درجة الدقة من 4 إلى 7؛ نتائج قابلة لإعادة الإنتاج والقياس.

### البند 1.5: إصلاحات الأداء العاجلة

- **الهدف:** إزالة أخطر عنق زجاجة دون إعادة هيكلة كبرى.
- **الخطوات التنفيذية:**
  1. حل N+1 في `engine.ts:411` بجلب دفعي واحد وبناء Map للعناوين.
  2. توحيد عملاء API كـ singletons على مستوى الوحدة.
  3. إضافة كاش LRU للتضمينات (مفتاح hash(model+text)).
  4. استبدال console بـ logger منظم (pino) بمستويات.
  5. جعل مقاييس التحليلات حقيقية (حساب p95 من الأزمنة المسجلة).
- **الأدوات والتقنيات:** pino، lru-cache، Redis (لاحقاً).
- **الأولوية:** 🟠 عالية (P1).
- **الأثر المتوقع:** خفض زمن الاستعلام 30–50%؛ مقاييس تشغيلية صادقة.

---

## 3. المرحلة الثانية — متوسطة المدى (الأشهر 2–3): "إعادة الهيكلة والاحتراف"

### البند 2.1: توحيد المعمارية على Next.js بالكامل

- **الهدف:** نقطة دخول واحدة وموجّه واحد ومصدر حقيقة واحد للبناء.
- **الخطوات التنفيذية:**
  1. ترحيل صفحات `views/` (react-router) إلى App Router أو حذف التجريبي منها.
  2. حذف `vite.config.ts` و`index.html` و`main.tsx` و`App.tsx` و`dev-server.js` بعد الترحيل.
  3. توحيد alias `@/*` ← `./src/*` في كل الإعدادات.
  4. توحيد الغلاف (Header واحد) والهوية (شعار واحد، إصدار واحد) واعتماد `server.ts` أو `next start` وحده.
- **الأدوات والتقنيات:** Next.js App Router، Next Middleware.
- **الأولوية:** 🟠 عالية (P1).
- **الأثر المتوقع:** إزالة الارتباك المعماري؛ خفض زمن فهم المشروع للمطور الجديد من أيام إلى ساعات؛ بناء واحد موثوق.

### البند 2.2: تقسيم الملفات العملاقة وإعادة الهيكلة

- **الهدف:** مكونات ووحدات أحادية المسؤولية قابلة للصيانة والاختبار (<300 سطر للملف).
- **الخطوات التنفيذية:**
  1. تقسيم `db.ts` إلى: `seed-data.ts` + `MemoryDatabase.ts` + `FirestoreDatabase.ts` + واجهة `IOmniRAGDatabase` مشتركة تفرض تطابق التوقيعات.
  2. استخراج نمط "التراجع للذاكرة" المكرر 25+ مرة في decorator/wrapper واحد (`withFallback`) مع إشعار صريح عند التراجع.
  3. تقسيم `ChatStudio.tsx` إلى خطافات (`useConversations`, `useChat`, `useCitations`) ومكونات عرض صغيرة؛ ونفس النمط لـ `SourcesDashboard` و`McpGateway` و`DocumentIngestionStudio`.
  4. استخراج منطق المحاكاة وبيانات العميل الخاص من `engine.ts` إلى وحدة `mocks/` معزولة تُحقن عبر DI وتُستبعد من الإنتاج.
  5. استخراج الثوابت السحرية (أوزان، عتبات، tenantId افتراضي) إلى ملف `config.ts` مركزي موثق.
- **الأدوات والتقنيات:** نمط Repository، Dependency Injection، مخرجات قياس التعقيد (madge/ts-prune).
- **الأولوية:** 🟠 عالية (P1).
- **الأثر المتوقع:** رفع قابلية الصيانة من 4.5 إلى 7.5؛ سرعة تطوير مضاعفة؛ سهولة ضم مطورين جدد.

### البند 2.3: إدارة حالة مركزية وجلب بيانات احترافي

- **الهدف:** إنهاء تشتت useState وتمرير props وسباقات الطلبات.
- **الخطوات التنفيذية:**
  1. اعتماد TanStack Query لجلب البيانات (كاش، إلغاء تكرار، إعادة محاولة، AbortController تلقائي).
  2. اعتماد Zustand لحالة الواجهة العابرة (التبويب النشط، اللغة، إعدادات النموذج).
  3. إلغاء الطلبات المتسابقة في ChatStudio وتوحيد حالات التحميل.
- **الأدوات والتقنيات:** TanStack Query v5، Zustand، React DevTools Profiler.
- **الأولوية:** 🟡 متوسطة (P2).
- **الأثر المتوقع:** واجهة أكثر استجابة وثباتاً؛ كود واجهة أقل بنسبة ~30%.

### البند 2.4: خط أنابيب استيعاب المستندات غير المتزامن

- **الهدف:** استيعاب مستندات كبيرة في ثوانٍ بدل دقائق، دون مهلات.
- **الخطوات التنفيذية:**
  1. نقل الاستيعاب إلى طابور خلفية: المسار يعيد `job_id` فوراً وحالة `processing`، وعامل (worker) ينفذ التقسيم والتضمين والفهرسة.
  2. Batch embedding لكل المقاطع في طلبات مجمعة، وFirestore batch writes، وupsert دفعي لـ Qdrant، وINSERT متعدد الصفوف لـ Postgres في معاملة.
  3. تقطيع دلالي حقيقي على حدود الجمل/الفقرات مع مراعاة الترقيم العربي وتداخل (overlap) محسوب.
  4. رفع الملفات عبر multipart streaming بدل base64-in-JSON، مع مؤشر تقدم لكل ملف وتوازي محدود (concurrency pool).
  5. تسجيل حالة فهرسة لكل مقطع (`indexed/failed`) مع إعادة محاولة أسّية ونقطة نهاية لإعادة الفهرسة.
- **الأدوات والتقنيات:** BullMQ + Redis (أو Cloud Tasks/PubSub)، Gemini batch embedding، tiktoken/عدّاد رموز عربي، postgres COPY.
- **الأولوية:** 🟠 عالية (P1).
- **الأثر المتوقع:** خفض زمن الاستيعاب 80–90%؛ إزالة خطر DoS من الرفع؛ موثوقية فهرسة قابلة للتتبع.

### البند 2.5: نظام تصميم موحد وواجهة إبداعية عصرية متاحة

- **الهدف:** هوية بصرية واحدة متسقة، وصولية WCAG 2.2 AA، وRTL احترافي كامل.
- **الخطوات التنفيذية:**
  1. بناء Design Tokens (ألوان، مسافات، طباعة، زوايا) في `tailwind.config` مخصص مع سمة داكنة/فاتحة ومبدّل سمة.
  2. مكتبة مكونات داخلية (Button, Input, Dialog, Tabs, Toast, Tooltip...) مبنية على Radix UI primitives مع shadcn/ui.
  3. تدقيق وصولية شامل: aria-label لكل زر أيقوني، أدوار ومعالم، تنقل كامل بلوحة المفاتيح، حلقات تركيز مرئية، رابط تخطٍّ، tooltips تعمل بالتركيز واللمس.
  4. إصلاح RTL منهجياً: استبدال الخصائص الفيزيائية بالمنطقية (`ms/me/ps/pe/start/end`)، حذف الكلاسات الوهمية `dir-rtl/dir-ltr`، ضبط `lang/dir` ديناميكياً.
  5. تحميل خط عربي احترافي (IBM Plex Sans Arabic أو Tajawal) وخط رياضي Amiri فعلياً عبر next/font.
  6. توحيد الشعار والإصدار والتسمية في كل الشاشات، وقائمة تنقل جوال (همبرغر) بمؤشرات واضحة.
  7. لمسات إبداعية: صفحات انتقالية بحركات spring، شاشات تحميل هيكلية (skeletons)، رسوم متحركة دقيقة للاستشهادات، وضع تركيز للمحادثة.
- **الأدوات والتقنيات:** Tailwind CSS v4 tokens، Radix UI + shadcn/ui، next/font، axe-core + jest-axe (اختبارات وصولية آلية)، Lighthouse CI، Storybook.
- **الأولوية:** 🟡 متوسطة (P2).
- **الأثر المتوقع:** رفع UI/UX من 6 إلى 8.5+؛ امتثال WCAG 2.2 AA يفتح أسواق المؤسسات والجهات الحكومية؛ تجربة عربية رائدة عالمياً.

### البند 2.6: جناح اختبارات وتقييم شامل

- **الهدف:** تغطية اختبارية >60% للوحدات الحرجة وقياس موضوعي لدقة الاسترجاع.
- **الخطوات التنفيذية:**
  1. اختبارات وحدة لكل الخطافات الـ12، خوارزمية RRF، التقطيع، التضمين، طبقة التخزين.
  2. اختبارات تكامل لمسارات API الحرجة (chat, documents, sources) عبر بيئة اختبار معزولة.
  3. اختبارات E2E لتدفقات: تسجيل الدخول ← رفع مستند ← محادثة مع استشهاد ← موافقة أداة.
  4. بناء مجموعة تقييم معلَّمة (Golden Q&A set عربية/إنجليزية) وحساب MRR/Recall@K/NDCG دورياً في CI.
  5. قياس p50/p95/p99 فعلية وتسجيلها في التحليلات بدل الثوابت.
- **الأدوات والتقنيات:** Vitest، Playwright، MSW (محاكاة API)، Firebase Emulator، Ragas أو تقييم مخصص، Lighthouse CI.
- **الأولوية:** 🟠 عالية (P1).
- **الأثر المتوقع:** ثقة كاملة في الإصدارات؛ دقة قابلة للإثبات بالأرقام أمام العملاء.

---

## 4. المرحلة الثالثة — طويلة المدى (الأشهر 4–6+): "التميز المؤسسي والتوسع"

### البند 3.1: قابلية الملاحظة والمراقبة الكاملة (Observability)

- **الهدف:** رؤية لحظية لصحة النظام والأداء والتكلفة والأمان.
- **الخطوات التنفيذية:** تتبع موزع لكل طلب (trace عبر API ← استرجاع ← توليد)، مقاييس RED لكل مسار، لوحات Grafana، تنبيهات SLO (p95 < 800ms، نسبة خطأ < 0.5%، نسبة فشل فهرسة < 1%)، تتبع تكلفة الرموز لكل مستأجر، إدارة أخطاء مركزية مع ربط بالإصدارات.
- **الأدوات والتقنيات:** OpenTelemetry، Sentry، Prometheus/Grafana (أو Better Stack/Axiom)، structured logging بـ pino.
- **الأولوية:** 🟡 متوسطة (P2).
- **الأثر المتوقع:** اكتشاف المشاكل قبل المستخدمين؛ قرارات تطوير مبنية على بيانات؛ جاهزية التزام SLA.

### البند 3.2: توسعة الأمان والامتثال المؤسسي

- **الهدف:** أمان من الطراز المؤسسي قابل للتدقيق الخارجي.
- **الخطوات التنفيذية:** تشفير اعتماديات الموصلات بـ AES-256-GCM بمفاتيح KMS، نقل الأسرار إلى مدير أسرار مركزي، صلاحيات RBAC دقيقة داخل المستأجر (مالك/محرر/قارئ)، اختبار اختراق دوري، سياسات احتفاظ بالبيانات وحذفها (GDPR)، سجلات تدقيق غير قابلة للعبث (hash-chained)، تقييد نطاقات CORS بدقة، CSP صارمة.
- **الأدوات والتقنيات:** Google Cloud KMS / Secret Manager، OWASP ZAP في CI، أدوات فحص SBOM (Syft) وتوقيع الاعتماديات (Sigstore).
- **الأولوية:** 🟡 متوسطة (P2).
- **الأثر المتوقع:** جاهزية اجتياز تدقيقات SOC 2/ISO 27001 مستقبلاً؛ ثقة عملاء المؤسسات.

### البند 3.3: موصلات MCP حقيقية وبيئة تنفيذ معزولة

- **الهدف:** تحويل أدوات MCP من محاكاة إلى قيمة حقيقية آمنة.
- **الخطوات التنفيذية:** تنفيذ موصلات Slack/GitHub/Notion/PostgreSQL حقيقية باعتماديات المستأجر المشفرة، بيئة sandbox فعلية لتنفيذ الأدوات (حاوية معزولة أو WebAssembly)، حصص لكل مستأجر (QuotaGuard حقيقي بقراءة usageMetadata)، تمييز مرئي صريح لأي بيانات محاكاة متبقية.
- **الأدوات والتقنيات:** Slack/GitHub/Notion SDKs، OAuth 2.1 للمستأجرين، Firecracker/Isolate أو WasmEdge للـ sandbox.
- **الأولوية:** 🟡 متوسطة (P2).
- **الأثر المتوقع:** فتح عروض قيمة جديدة (أتمتة سير العمل)؛ مصداقية كاملة في مخرجات الأدوات.

### البند 3.4: قابلية التوسع والأداء المؤسسي

- **الهدف:** دعم آلاف المستأجرين وملايين المقاطع بزمن استجابة ثابت.
- **الخطوات التنفيذية:** كاش استعلامات متعدد المستويات (Redis للتضمينات والنتائج الشائعة)، CDN للأصول وNext.js ISR حيث يناسب، read replicas لـ Postgres وتقسيم Qdrant، تحديد توازي ذكي وحماية من التحميل (circuit breakers)، ميزانية أداء للواجهة (<200KB JS أولي، LCP < 2.5s) مراقبة آلياً.
- **الأدوات والتقنيات:** Redis/Upstash، Neon read replicas، bundle analyzer + size-limit، Web Vitals.
- **الأولوية:** 🟢 مخططة (P3).
- **الأثر المتوقع:** زمن استجابة ثابت تحت الحمل؛ خفض تكاليف API 40%+ عبر الكاش.

### البند 3.5: تجربة مستخدم إبداعية من الجيل التالي

- **الهدف:** جعل OmniRAG المرجع العربي الأول لتجربة منصات الذكاء الاصطناعي.
- **الخطوات التنفيذية:** بث تدفقي حقيقي معمم (SSE) مع إيقاف/استئناف، وضع محادثة صوتي كامل (STT/TTS عربي)، مقارنة نماذج جنباً إلى جنب، ساحة استرجاع تفاعلية برسوم بيانية حية لنتائج الدمج الهجين، اختصارات لوحة مفاتيح شاملة ولوحة أوامر (⌘K)، تخصيص سمات لكل مستأجر (white-labeling)، PWA للعمل دون اتصال جزئياً.
- **الأدوات والتقنيات:** Streaming AI SDK، Web Speech API/موفر TTS عربي، cmdk، Recharts/D3، Workbox.
- **الأولوية:** 🟢 مخططة (P3).
- **الأثر المتوقع:** تمايز تنافسي حاسم؛ رفع معدلات التفعيل والاحتفاظ بالمستخدمين.

### البند 3.6: هندسة إصدارات وعمليات ناضجة

- **الهدف:** دورة إصدار أسبوعية آمنة بلا توقف.
- **الخطوات التنفيذية:** بيئات متعددة (dev/staging/prod) مع migrations مُدارة، feature flags لكل ميزة جديدة، نشر تدريجي (canary) مع تراجع فوري، إصدارات دلالية آلية وCHANGELOG مولّد، مراجعات كود إلزامية وCODEOWNERS، توثيق API تفاعلي مولّد.
- **الأدوات والتقنيات:** Drizzle/Prisma migrations (أو SQL migrations)، Flagsmith/Unleash، Vercel/Cloud Run canary، semantic-release، OpenAPI + Scalar.
- **الأولوية:** 🟢 مخططة (P3).
- **الأثر المتوقع:** سرعة تسليم مستدامة بثقة؛ تقليل حوادث الإصدار إلى الصفر تقريباً.

---

## 5. مؤشرات الأداء الرئيسية للخطة (KPIs)

| المؤشر | الوضع الحالي | الهدف بعد المرحلة 1 | الهدف بعد المرحلة 2 | الهدف بعد المرحلة 3 |
|---|---|---|---|---|
| ثغرات حرجة/عالية مفتوحة | 7 | 0 | 0 | 0 |
| تغطية الاختبارات (الوحدات الحرجة) | 0% | 30% | 60%+ | 75%+ |
| درجة الأمان (فحص داخلي) | 2/10 | 8/10 | 9/10 | 9.5/10 |
| درجة جودة الكود | 4.5/10 | 6.5/10 | 7.5/10 | 8.5/10 |
| درجة UI/UX | 6/10 | 6.5/10 | 8.5/10 | 9/10 |
| امتثال WCAG 2.2 | غير ممتثل | A جزئي | AA | AA |
| p95 لزمن استعلام المحادثة | غير مقاس (وهمي 240ms) | مقاس فعلياً | < 1.2s | < 800ms |
| زمن استيعاب مستند 50 مقطعاً | دقائق | دقائق | < 30s | < 15s |
| Recall@K (مجموعة التقييم) | غير قابل للقياس (عشوائي) | قابل للقياس | ≥ 0.85 | ≥ 0.92 |
| عدد الملفات > 500 سطر | 5 | 5 | 0 | 0 |

---

## 6. مصفوفة المخاطر والتخفيف

| الخطر | الاحتمال | الأثر | التخفيف |
|---|---|---|---|
| كسر وظائف قائمة أثناء إعادة الهيكلة | متوسط | عالٍ | اختبارات المرحلة 1 تسبق التقسيم؛ feature flags؛ ترحيل تدريجي |
| اكتشاف تسريبات إضافية في تاريخ Git | متوسط | عالٍ | git-filter-repo شامل + تدوير كل الأسرار دون استثناء |
| فقدان بيانات مستخدمين أثناء توحيد التخزين | منخفض | عالٍ | نسخ احتياطية إلزامية + ترحيلات مُختبرة على staging + وضع قراءة فقط أثناء النقل |
| تأخر الموصلات الحقيقية (تعقيد OAuth لكل خدمة) | متوسط | متوسط | البدء بموصلين فقط (Slack/GitHub) وإبقاء المحاكاة موسومة بوضوح |
| مقاومة تغيير الهوية البصرية | منخفض | منخفض | توثيق نظام التصميم في Storybook ومراجعته مع أصحاب المصلحة مبكراً |

---

## 7. خارطة الطريق الزمنية الملخصة

- **الأسابيع 1–4 (قصيرة المدى):** أمن حرج (1.1) ← ترميز عربي (1.2) ← بنية جودة (1.3) ← دقة الاسترجاع (1.4) ← أداء عاجل (1.5).
- **الأشهر 2–3 (متوسطة المدى):** توحيد Next.js (2.1) ← تقسيم الملفات العملاقة (2.2) ← إدارة الحالة (2.3) ← خط استيعاب غير متزامن (2.4) ← نظام التصميم والوصولية (2.5) ← جناح الاختبارات والتقييم (2.6).
- **الأشهر 4–6+ (طويلة المدى):** المراقبة (3.1) ← الامتثال المؤسسي (3.2) ← موصلات MCP حقيقية (3.3) ← التوسع والأداء (3.4) ← تجربة الجيل التالي (3.5) ← عمليات الإصدار (3.6).

**النتيجة النهائية المتوقعة:** منصة OmniRAG إنتاجية بدرجة إجمالية ≥ 8.5/10 — آمنة افتراضياً، دقيقة قابلة للإثبات، سريعة قابلة للتوسع، سهلة الصيانة والتطوير، وبواجهة عربية-عالمية رائدة في فئتها.

---

*أُعد هذا التقرير بدمج نتائج الفحوصات الخمسة: تحليل البنية والمعمارية، تقييم جودة الكود، التدقيق الأمني، تقييم واجهة وتجربة المستخدم، وتحليل الأداء والدقة — بتاريخ 2026-08-11.*
