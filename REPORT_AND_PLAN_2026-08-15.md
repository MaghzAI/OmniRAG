# 📋 التقرير الشامل الاحترافي — منصة OmniRAG (v0.4.4)

> **تاريخ الفحص:** 2026-08-15
> **نطاق الفحص:** بنية المشروع كاملة (~85 ملف TypeScript، 33 مسار API، 31 مكوّن React)
> **المنهجية:** فحص استكشافي كامل للبنية + قراءة معمّقة للملفات المحورية (المصادقة، محرك RAG، طبقة الحماية، التخزين) + تشغيل فعلي للاختبارات + مقارنة مع تقرير التدقيق السابق (2026-08-11)

---

## 1. الملخص التنفيذي

OmniRAG منصة RAG مؤسسية متعددة المستأجرين مبنية على Next.js 16 + React 19 + Gemini + Qdrant + PostgreSQL + Firebase. المشروع يمتلك **رؤية معمارية ناضجة** وتوثيق SDLC استثنائي، وقد شهد **تقدماً حقيقياً** منذ التدقيق السابق: خوارزمية RRF أصبحت حقيقية، وبُنية المصادقة وRate Limiting أُنشئت، والتشفير العربي أُصلح.

**لكن الفحص الفعلي كشف: البنية التحتية الأمنية الجديدة هي "أمن شكلي" (Security Theater)** — فالمصادقة يمكن تجاوزها بثلاث طرق، وقواعد Firestore ما زالت مفتوحة للعالم، والاختبارات **معطلة فعلياً** (2 من 3 ملفات تفشل في التحميل)، والملفات العملاقة **كبرت بدل أن تتقلص**. المشروع لا يزال غير صالح للنشر الإنتاجي.

### جدول التقييم المحدّث

| المحور              | السابق (11/8) | **الحالي (15/8)** | الاتجاه | السبب                                                             |
| ------------------- | ------------- | ----------------- | ------- | ----------------------------------------------------------------- |
| البنية والمعمارية   | 6.5           | **6.5**           | ➡️      | ازدواجية التوجيه قائمة، ميزات جديدة أُضيفت بانضباط                |
| جودة الكود والصيانة | 4.5           | **5.5**           | ⬆️      | RRF حقيقي + إزالة تكرار البيانات الأولية، لكن ملفات الواجهة تضخمت |
| الأمان              | 2.0           | **3.0**           | ⬆️ طفيف | بنية أمنية أُضيفت لكنها قابلة للتجاوز بالكامل                     |
| UI/UX               | 6.0           | **6.0**           | ➡️      | ميزات جديدة دون معالجة الوصولية/RTL                               |
| الأداء              | 4.0           | **4.5**           | ⬆️      | إصلاح N+1، لكن بقية عناقيق الزجاجة قائمة                          |
| الدقة               | 4.0           | **6.0**           | ⬆️⬆️    | RRF حقيقي حتمي + فصل HyDE + إزالة العشوائية                       |
| الاختبارات          | 0             | **2/10**          | ⬆️      | اختبارات وُجدت لكن 2/3 منها معطلة بسبب اعتماديات مفقودة           |
| **المتوسط العام**   | 4.5           | **~5.1**          | ⬆️      | **نموذج أولي متقدم يتحسن، لكنه غير إنتاجي**                       |

---

## 2. ما تم إصلاحه فعلياً منذ التقرير السابق ✅

تم التحقق من الكود مباشرة، وهذه إصلاحات **حقيقية وموثقة**:

1. **RRF حقيقي:** `computeRrfScore` في `engine.ts:282-297` تطبق الصيغة الصحيحة `Σ 1/(60+rank) × weight` مع فرز حتمي عند التعادل (`engine.ts:396`).
2. **فصل HyDE:** نص HyDE يُستخدم للفرع الدلالي فقط، والاستعلام الأصلي النظيف للفرع المعجمي (`engine.ts:306-315`).
3. **إصلاح N+1:** عناوين المستندات تُجلب دفعة واحدة عبر Map (`engine.ts:375-381`).
4. **حتمية المسار الاحتياطي:** إزالة `Math.random()` من درجات البحث الاحتياطي (`engine.ts:442-443`).
5. **إصلاح الترميز العربي:** أنماط كشف حقن الـ Prompt في `hook-harness.ts:28-29` أصبحت UTF-8 سليمة.
6. **بنية أمنية:** `withAuthAndRateLimit` مطبق على 22 من 33 مساراً، مع `rateLimiter` يرجع 429 مع `Retry-After`.
7. **إلغاء تكرار البيانات الأولية:** `db.ts` (1439 سطراً بدل 1628) يستورد من `constants.ts` كمصدر وحيد.
8. **وحدات جديدة:** تشفير AES-256-GCM، مغلف `withFallback`، مدير OAuth 2.1 مع PKCE، Gemini مقاوم للأعطال، كاش LRU لـ Mistral OCR.
9. **ميزات جديدة:** Mistral OCR، Unstructured API، Groq Whisper، تحليل DOCX.

---

## 3. الثغرات الحرجة القائمة (متحقق منها بالكود) 🔴

### C1 — المصادقة شكلية وقابلة للتجاوز بثلاث طرق (`src/lib/auth/apiAuth.ts`)

- **بدون ترويسة Authorization** → `authenticated: true` مع tenant من الـ query string (يتحكم به المهاجم) — `apiAuth.ts:21-28`
- **`Bearer tenant-anything`** → انتحال فوري لأي مستأجر — `apiAuth.ts:32-39`
- **فشل التحقق من Firebase Token** → تراجع تلقائي إلى `authenticated: true` بدل رفض — `apiAuth.ts:55-63`

**النتيجة:** ثغرة BOLA/IDOR ما زالت قابلة للاستغلال بالكامل، مع وهم الحماية.

### C2 — المعالجات تتجاهل هوية المصادقة

حتى المسارات المغلَّفة تقرأ `tenantId` من جسم الطلب وليس من `authCtx`:

- `search/route.ts:22` و`mcp/servers/[id]/route.ts:37` و`mcp/oauth/initiate/route.ts:7`: `body.tenantId || headers || 'tenant-alpha-001'`
- `sources/route.ts` (6 مواضع): افتراضي `'tenant-acme-01'` من العميل
- `conversations/route.ts:11`: من query string مباشرة

### C3 — قواعد Firestore مفتوحة للعالم (`firestore.rules:6`)

`allow read, write: if true` — أي شخص على الإنترنت يقرأ/يحذف كامل قاعدة البيانات.

### C4 — CORS يعكس أي أصل مع بيانات الاعتماد (`middleware.ts:5-16`)

`Access-Control-Allow-Origin: <أي origin>` + `Access-Control-Allow-Credentials: true`.

### C5 — الاختبارات معطلة فعلياً (تحقق بالتشغيل)

`vitest`: **2 من 3 ملفات فشلت في التحميل** — `youtube-transcript` و`zod` غير مثبتين في node_modules رغم إعلانهما. وجود قفلين متضاربين (`package-lock.json` + `bun.lock`) = بيئة غير قابلة لإعادة الإنتاج.

---

## 4. مشاكل عالية ومتوسطة الخطورة

| #   | المشكلة                                                                                                  | الدليل                                                       | التصنيف   |
| --- | -------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------ | --------- |
| H1  | حد رفع الملفات **200MB** قابل للتحكم من العميل عبر ترويسة                                                | `documents/parse/route.ts:102,145`                           | DoS       |
| H2  | TLS معطل في 3 مواضع                                                                                      | `postgres.ts:61`, `migrate.ts:18`, `env-config/route.ts:235` | MITM      |
| H3  | `encryption.ts` و`withFallback.ts` **كود ميت** — كُتبا ولم يُربطا                                        | grep أرجع الملفين فقط                                        | أمن زائف  |
| H4  | 11 مساراً بلا مغلف حماية (`genai`, `mcp/[...path]`, `mcp/calls`, `mcp/health`, oauth, `sdlc-analyze`...) | مقارنة قائمتين                                               | تغطية     |
| H5  | Rate limiter في الذاكرة فقط — لا يعمل مع نسخ متعددة                                                      | `rateLimiter.ts:7`                                           | توسع      |
| H6  | `Math.random()` للمعرفات في `db.ts` (4)، `hook-harness.ts:166`، `engine.ts`، `oauth-manager.ts`          | قابل للتنبؤ                                                  | صحة       |
| H7  | `ignoreBuildErrors: true` مفعّل + `@google/genai: "*"` غير مثبت                                          | `next.config.ts:7`, `package.json:18`                        | جودة      |
| M1  | خطأ PII Redactor: `regex.test()` مع علم `/g` يسبب `lastIndex` متقدماً → تفويت بالتناوب                   | `hook-harness.ts:145-151`                                    | دقة أمنية |
| M2  | `pre_auth` يتحقق من "وجود نص tenantId" فقط — لا يربطه بهوية                                              | `hook-harness.ts:66`                                         | أمن       |
| M3  | نمط "12 خطافاً" انخفض فعلياً إلى ~8 فحوصات                                                               | `hook-harness.ts` (177 سطراً)                                | توثيق     |
| M4  | المسار الاحتياطي يحمّل كل مقاطع المستأجر في الذاكرة                                                      | `engine.ts:410`                                              | أداء      |
| M5  | سكربتات npm بصيغة Unix (`rm -rf`) لا تعمل على Windows                                                    | `package.json:9-11`                                          | تشغيل     |
| M6  | ازدواجية توجيه كاملة (Next.js + Vite/React Router)                                                       | `main.tsx`, `App.tsx`, `index.html`                          | معمارية   |

---

## 5. جودة الكود — اتجاه مقلق في الواجهة

| الملف                         | قبل (11/8) | **الآن (15/8)** | الاتجاه  |
| ----------------------------- | ---------- | --------------- | -------- |
| `DocumentIngestionStudio.tsx` | 1127       | **1853**        | 🔴 +64%  |
| `ChatStudio.tsx`              | 1497       | **1626**        | 🔴 +9%   |
| `SourcesDashboard.tsx`        | 1477       | **1611**        | 🔴 +9%   |
| `McpGateway.tsx`              | 1141       | **1524**        | 🔴 +34%  |
| `db.ts`                       | 1628       | **1439**        | 🟢 −12%  |
| `engine.ts`                   | —          | **717**         | 🟢 منظّم |

الميزات الجديدة أُضيفت فوق مكوّنات عملاقة بدل تقسيمها — God Components تتفاقم.

---

# 🗺️ الخطة الاحترافية الشاملة للإصلاح والتطوير

**الرؤية:** تحويل OmniRAG إلى منصة إنتاجية عالمية — آمنة افتراضياً، دقيقة قابلة للإثبات، سريعة قابلة للتوسع، سهلة الصيانة، بتجربة عربية رائدة.
**المبدأ الحاكم:** لا ميزة جديدة قبل إغلاق الثغرات الحرجة. كل بند قابل للشحن المستقل.

## المرحلة صفر — تصحيح المسار (الأيام 1–3) 🔴 P0

1. **إعادة كتابة `apiAuth.ts` بمنطق رفض صارم:** لا توكن → 401. توكن غير صالح → 401. `tenant-*` في Bearer → رفض. المصدر **الحصري** للهوية هو `decodedToken.uid` (مع وضع تطوير صريح اختياري عبر متغير بيئة).
2. **ربط المعالجات بـ `authCtx.tenantId`:** حذف `body.tenantId`/query/header من كل مسارات v1، وتغليف المسارات الـ11 العارية.
3. **إصلاح CORS:** قائمة أنواع مسموحة من `ALLOWED_ORIGINS`؛ رفض الأصل `null`.
4. **نشر `firestore.rules` حقيقية** مربوطة بـ `request.auth` مع عزل مستأجرين، واختبارها بمحاكي Firebase.
5. **إصلاح بيئة البناء:** حذف `bun.lock`، توحيد npm، تثبيت `zod` و`youtube-transcript`، تقييد `@google/genai`، التأكد أن `vitest run` يمر 3/3.
6. **إسقاط حد الرفع إلى 10MB افتراضياً و50MB سقفاً** بضوابط خادمة.
7. **تفعيل TLS:** `rejectUnauthorized` مع CA موثوقة في المواضع الثلاثة.
8. **ربط الكود الميت:** تشفير اعتماديات MCP فعلياً عبر `encryption.ts`، واعتماد `withFallback.ts` في `db.ts`.

**مخرَج المرحلة:** صفر ثغرات حرجة قابلة للاستغلال + اختبارات تعمل + بناء قابل لإعادة الإنتاج.

## المرحلة الأولى — الأساس (الأسابيع 1–4) 🔴 P0-P1

1. **جدار جودة آلي:** ESLint + Prettier + `ignoreBuildErrors: false`، GitHub Actions (lint → typecheck → test → audit → gitleaks)، Husky + lint-staged، Dependabot.
2. **اختبارات تحمي الإصلاحات:** وحدات لـ `apiAuth` (حالات التجاوز كاختبارات رفض)، `computeRrfScore` بقيم متوقعة، PII Redactor، حدود Rate Limiter.
3. **حتمية كاملة:** `crypto.randomUUID()` بدل كل `Math.random()` في منطق الخادم.
4. **سكربتات متعددة المنصات:** `rimraf` و`cross-env`.
5. **أسرار المخرجات:** رسائل خطأ عامة للعميل + تفاصيل داخلية؛ تنقية `/api/health`.

## المرحلة الثانية — الإنتاجية (الأشهر 2–3) 🟠 P1-P2

1. **تقسيم المكوّنات العملاقة:** خطافات `useConversations/useChat/useIngestion` ومكونات عرض < 300 سطراً؛ هدف صفر ملف > 500 سطر.
2. **تقسيم `db.ts`:** واجهة `IOmniRAGDatabase` + `MemoryDatabase` + `FirestoreDatabase` + Postgres.
3. **توحيد المعمارية على Next.js وحده:** حذف Vite/React Router، alias موحّد.
4. **خط استيعاب غير متزامن:** طابور خلفي + Batch embedding + كتابات دفعية — 50 مقطعاً في < 30 ثانية.
5. **موصلات MCP حقيقية** (تبدأ بـ GitHub وSlack) مع تمرير كل استدعاء عبر `pre_tool`.
6. **إدارة حالة:** TanStack Query + Zustand.
7. **نظام تصميم ووصولية:** Design Tokens، Radix، aria شامل، RTL منطقي، WCAG 2.2 AA.
8. **مقاييس حقيقية:** p50/p95 فعلية، Recall@K على مجموعة تقييم معلَّمة في CI.

## المرحلة الثالثة — التميز المؤسسي (الأشهر 4–6+) 🟡 P2-P3

1. **Observability:** OpenTelemetry + Sentry، تتبع موزع، تنبيهات SLO.
2. **أمان مؤسسي:** RBAC داخل المستأجر، سجلات تدقيق hash-chained، جاهزية SOC 2/ISO 27001، Rate limiting موزع (Redis).
3. **توسع:** كاش Redis متعدد المستويات، read replicas، ميزانية أداء مراقبة.
4. **تجربة الجيل التالي:** SSE معمم مع إيقاف/استئناف، وضع صوتي عربي، مقارنة نماذج، ⌘K، PWA.
5. **هندسة إصدارات:** dev/staging/prod، migrations مُدارة، feature flags، نشر تدريجي، OpenAPI تفاعلي.

## مؤشرات النجاح (KPIs)

| المؤشر                     | الآن      | بعد م0+م1         | بعد م2 | بعد م3 |
| -------------------------- | --------- | ----------------- | ------ | ------ |
| ثغرات حرجة قابلة للاستغلال | 5         | **0**             | 0      | 0      |
| اختبارات تعمل/تغطية حرجة   | 1/3 ملفات | 3/3 و30+ اختباراً | 60%+   | 75%+   |
| مسارات تستخدم هوية موثقة   | 0 من 33   | **33 من 33**      | 33     | 33     |
| ملفات > 500 سطر            | 5+ وتنمو  | 5                 | **0**  | 0      |
| Recall@K قابل للقياس       | لا        | قابل للقياس       | ≥ 0.85 | ≥ 0.92 |

---

### الخلاصة

المشروع يتحرك بالاتجاه الصحيح وبوتيرة جيدة (5 إصلاحات جوهرية في 4 أيام)، لكن **الإصلاح الأمني الأهم نُفذ شكلياً** ويجب إعادة بنائه من جذوره (apiAuth + ربط المعالجات بالهوية + قواعد Firestore + CORS)، وبيئة البناء مكسورة (اختبارات معطلة + قفلان متضاربان). المرحلة صفر (3 أيام عمل) هي نقطة البداية الصحيحة قبل أي شيء آخر.

---

_أُعد هذا التقرير بفحص مباشر للكود وتشغيل الاختبارات — بتاريخ 2026-08-15._
